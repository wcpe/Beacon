# Beacon agent 构建属性（被根 build.gradle.kts 与各子模块读取）。

# 工件坐标的 group，所有子模块共用。
group=com.beacon.agent
# agent 版本号，M0 暂随主项目处于 0.1.0。
version=0.1.0

# Gradle 守护进程 JVM 参数：放宽堆内存，统一文件编码为 UTF-8。
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
