# 构建产物（由 vite build 生成，不入格式化范围）
dist
apps/*/dist
packages/*/dist
# 测试覆盖率报告
coverage
# 依赖锁文件（由 pnpm 维护，禁手改格式）
pnpm-lock.yaml
# 第三方依赖
node_modules
# Playwright E2E 产物（报告 / 结果 / 真后端运行目录），不入格式化范围
test-results
playwright-report
.e2e-real-run
