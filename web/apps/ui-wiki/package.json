{
  "name": "@beacon/ui-wiki",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node ../../scripts/check-ui-wiki-coverage.mjs && tsc -p tsconfig.json --noEmit && vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@beacon/ui": "workspace:*"
  }
}
