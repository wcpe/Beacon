// 版本与更新页（/system/version）：占位骨架，由对应页面 agent 以真实内容替换本文件
import PageScaffold from './page-scaffold'

export default function SystemVersionPage() {
  return <PageScaffold titleKey="nav.systemVersion" missionKey="system.version.mission" />
}
