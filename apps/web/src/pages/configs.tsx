// 配置中心页（/configs）：占位骨架，由对应页面 agent 以真实内容替换本文件
import PageScaffold from './page-scaffold'

export default function ConfigsPage() {
  return <PageScaffold titleKey="nav.configs" missionKey="delivery.configs.mission" />
}
