// 交付历史页（/changes/history）：占位骨架，由对应页面 agent 以真实内容替换本文件
import PageScaffold from './page-scaffold'

export default function ChangesHistoryPage() {
  return <PageScaffold titleKey="nav.changesHistory" missionKey="delivery.changesHistory.mission" />
}
