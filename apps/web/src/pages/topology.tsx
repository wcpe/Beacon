// 拓扑页（/topology）：占位骨架，由对应页面 agent 以真实内容替换本文件
import PageScaffold from './page-scaffold'

export default function TopologyPage() {
  return <PageScaffold titleKey="nav.topology" missionKey="cluster.topology.mission" />
}
