// 服务器页（/servers）：占位骨架，由对应页面 agent 以真实内容替换本文件
import PageScaffold from './page-scaffold'

export default function ServersPage() {
  return <PageScaffold titleKey="nav.servers" missionKey="cluster.servers.mission" />
}
