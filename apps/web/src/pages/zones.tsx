// 区服分配页（/zones）：占位骨架，由对应页面 agent 以真实内容替换本文件
import PageScaffold from './page-scaffold'

export default function ZonesPage() {
  return <PageScaffold titleKey="nav.zones" missionKey="cluster.zones.mission" />
}
