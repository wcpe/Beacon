# BeaconAgentProxy（Bungee/BC）本地引导配置。
# 只填写控制面地址和 namespace 接入 token；所有 listener 会自动上报，首次落脚与拓扑由控制面管理。
# 需要调整本地运行参数时，按 docs/wiki/bc-agent-full-config.md 增量添加；不要恢复已废弃的本地路由字段。
beacon:
  endpoints:
    - "<CONTROL_PLANE_URL>"
  bootstrap-token: "<NAMESPACE_ACCESS_TOKEN>"
