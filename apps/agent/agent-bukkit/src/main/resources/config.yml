# BeaconAgent（Bukkit/Paper）本地引导配置。
# 只填写控制面地址和 namespace 接入 token；身份、地址、拓扑与业务配置由控制面管理。
# 需要调整本地运行参数时，按 docs/wiki/bukkit-agent-full-config.md 增量添加，不要复制其它服务器的状态目录。
beacon:
  endpoints:
    - "<CONTROL_PLANE_URL>"
  bootstrap-token: "<NAMESPACE_ACCESS_TOKEN>"
