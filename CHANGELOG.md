# 更新日志

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/) 与[语义化版本](https://semver.org/lang/zh-CN/)。

## 未发布

### 新增
- 控制面更新出站代理 + 客户端工厂 + proxy-url 设置项 + 凭据脱敏（FR-98，feat，控制面，见 [ADR-0047](docs/adr/0047-update-outbound-proxy-and-secret-redaction.md)，扩展 [ADR-0038](docs/adr/0038-ops-settings-store-hot-reload.md)）：为后续控制面在线自更新（FR-97/FR-99）的公网出站铺底。**① 出站客户端工厂**：新增极薄包 `internal/httpx`，把「构造带代理 + 超时的 `*http.Client`」收口到单一小工厂 `NewClient(proxyURL, timeout)`——非空代理→`http.Transport{Proxy: http.ProxyURL(parsed)}`、空→直连；**只支持 http/https 正向代理**（标准库原生，不引 socks5 / 不引新依赖 / 不读 `*_PROXY` 环境变量，YAGNI），**明确不照搬 ADR-0005 的 agent transport 接口抽象**（那是 agent 防库冲突的约束，控制面用标准库即足）。**② 热改设置项 `update.proxy-url`**：设置 store 白名单（FR-61）新增该 string 项（归「网络代理」语义），值为代理地址（`http(s)://host:port`，可含 `user:pass`），**空=直连**；应用层校验 scheme∈{http,https} + host:port（与工厂 `ParseProxyURL` 同口径），`config.yml` 增首启 seed 默认空。**③ 作用域硬约束**：代理**仅作用于「更新相关出站」**，**不改 `internal/runtime/alert/webhook.go` 既有裸连行为**（向后兼容）；本批无更新消费者（FR-97 后续批），故只交付工厂 + store 项 + 脱敏 + 单测，不 wire 不存在的 updater、不动 webhook。**④ 凭据脱敏（最高风险）**：`update.proxy-url` 是 store 首个含凭据项，打破 ADR-0038「白名单不含密钥故 value 可明文记」前提——新增 Go URL 凭据脱敏纯函数 `httpx.RedactURLCredentials`（精确掩 userinfo 段，`http://user:pass@h`→`http://***:***@h`、仅用户名→`http://***@h`、无凭据不误掩、空串返空、解析失败宁严勿松整体掩 `***`，穷举单测），并在**审计 detail / slog 日志 / 前端回显**三处全脱敏、**落库存原值仅供运行**；前端「未改密码」语义=提交脱敏占位则后端保留原值不覆盖。**经代理真连 GitHub 的真机维度待 FR-97 接入后验。**
- 内置 launcher 监督进程（FR-96，feat，控制面，无 agent 改动，见 [ADR-0045](docs/adr/0045-builtin-launcher-supervisor.md) 与 [docs/specs/online-update.md](docs/specs/online-update.md)）：新增独立第二二进制 `beacon-launcher`（`cmd/beacon-launcher`，与主二进制同仓、版本经同一 `-ldflags -X internal/version.Version` 注入）作常驻监督进程，使控制面无需外部 systemd / docker 即**崩溃自动重启**。launcher **极薄**（仅标准库 `os/exec` + `os`、不连 DB、不碰业务、零第三方依赖）：以子进程方式启动同目录 `beacon[.exe]`（透传 `-config` 等参数与全部环境变量、继承 stdout/stderr/stdin），按**退出码协议**决策——退出码常量集中 `internal/exitcode`、主进程与 launcher **共享引用**（防魔法数字散落）：`0`=正常退出则 launcher 跟随退出不重启；`1` 及一切非零（含信号退出 `128+signum`，如 SIGINT=130/SIGKILL=137）=崩溃，按**固定间隔 3s 重启 + 连续失败上限 5 次**超限即停打 ERROR（不指数退避、不套 sysexits）；`70`=请求更新重启，用主进程落位的 pending 新二进制做**跨平台原子换二进制**后重启（**Windows** 旧 `beacon.exe` rename 为 `.old` 让位 → pending `beacon.new.exe` rename 就位 → 删旧，绕开运行中 exe 文件锁；**Linux** rename 直接覆盖；任一步失败 / pending 缺失则**保留旧二进制、按旧版重启并打 WARN**，回滚兜底）。端口**先退后起**（主进程 graceful shutdown 释放端口 → 退出 → launcher 重启新进程重新监听同端口），亚秒窗口由 agent fail-static 兜、玩家进服不受影响。主进程 `cmd/beacon` 侧把现有 `os.Exit(1)` 与正常返回纳入退出码协议常量，并**新增「请求更新重启」出口常量**（哨兵 `errRequestUpdateRestart` 映射到 `exitcode.RequestUpdateRestart`）——本批仅备好这条退出出口，实际更新逻辑（查 Release / 下载 / 校验 / 落位 pending）在 FR-97 实现；现有 SIGINT/SIGTERM 优雅关停段不变。**裸跑 `beacon`（不经 launcher）仍完全可用**，只退化为无自动重启（需手动重启），launcher 是可选监督外壳、非运行前提。被否方案：在 `main.go` 内 fork 自监督（Windows 运行中 exe 文件锁致自换必败，换文件必须由不被替换的进程做）。仅控制面、不涉 agent jar；Makefile / Dockerfile / release workflow 适配在 FR-102。
- 更新设置项 + 网络代理 / 更新设置子 tab UI（FR-101，feat，控制面 + 前端无 agent 改动，沿用 [ADR-0038](docs/adr/0038-ops-settings-store-hot-reload.md)/[ADR-0047](docs/adr/0047-update-outbound-proxy-and-secret-redaction.md)）：为后续控制面在线自更新（FR-99 消费）补更新渠道与自动检查旋钮，并把 FR-94 预留的两个空壳子 tab 填实。**① 后端 3 个新热改项**（设置 store 白名单，FR-61）：`update.channel`（string 枚举 `stable`/`rc`，默认 `stable`）、`update.auto-check-enabled`（bool，默认 `true`）、`update.check-interval-hours`（int，下界 1 上界 168，默认 6），归「更新设置」语义，经既有类型 / 枚举 / 范围校验 + 审计 `settings.update`（均非敏感、直记），`config.yml` 增三项首启 seed 默认。`UpdateConfig` 结构相应加 `Channel`/`AutoCheckEnabled`/`CheckIntervalHours` 字段。热改白名单 13→16 项。**② 前端两子 tab 填实**：把 FR-94 预留的系统设置块「网络代理」「更新设置」空壳子 tab 改为逐项编辑——网络代理子 tab 编辑 `update.proxy-url`（FR-98 脱敏回显口径：GET 回脱敏串、未改不回传覆盖原值，已由后端落实）、更新设置子 tab 编辑 channel/auto-check-enabled/check-interval-hours（channel 走固定枚举下拉 stable/rc）。**复用 FR-62/FR-77 既有逐项编辑控件 / 校验 / 草稿 / 批量保存范式**：把原 OpsSettingsBlock 内私有的 `SettingRow`/`SettingControl`/`ChangeSummary` 与集中草稿 + dirty + 批量保存逻辑上提为共享 `settingsEditing.tsx`（消除跨块复制），OpsSettingsBlock 与 SystemConfigBlock 各组合复用；网络代理 + 更新设置两子 tab **共享同一份草稿与块内统一批量保存条**（跨子 tab 统一保存不回归）。**范围纪律**：只做这 3 项设置 + 2 子 tab 编辑 UI，**不做自动下载 / 安装 / 触发**（属在线更新执行，是 FR-97/99/100）。三子 tab 功能 / 代理脱敏回显交互 / 命令面板深链待真机验。
- 控制面在线更新核心（FR-97，feat，控制面，依赖 FR-96/FR-98，见 [ADR-0044](docs/adr/0044-control-plane-online-self-update.md) 与 [docs/specs/online-update.md](docs/specs/online-update.md)）：在 FR-96 launcher 监督 + 退出码协议、FR-98 出站工厂之上实现主进程侧**更新核心**——**仅控制面单二进制**（含 `go:embed` 前端整体替换，agent jar 不在范围）。新增 `internal/update`：**① 按渠道查 Release**：渠道（`stable`/`rc`）作**入参**（不读 store——store 渠道项由 FR-101 加、FR-99 后续批读后传入；本批不依赖 FR-101），查 wcpe/Beacon GitHub Releases（仓址可配、默认此值），`stable`=最新非 prerelease、`rc`=最新 prerelease，与 `version.Version` 比较（**自写最小 semver 比较器**含 `-rc.N` 预发布序、**不引第三方 semver 库**；`dev` 哨兵视为未知不提示）。**② 下载资产**：按本平台 `runtime.GOOS/GOARCH` 选 `beacon-<ver>-<os>-<arch>[.exe]`（资产集 **5 平台** linux-amd64/arm64·windows-amd64·darwin-amd64/arm64，仅已发布平台可自更新）下载到运行目录旁临时文件，**超时 + 大小上限（`io.LimitReader`）+ 任何失败 `os.Remove` 临时文件**（资源泄露禁令），出站**经 `internal/httpx` 工厂**（带代理入参 + 超时，不裸用 `net/http`）。**③ SHA256 校验**：下载 release 的 `SHA256SUMS.txt` 比对实算 SHA256，不通过即中止、删临时文件、状态 `failed`、不替换、**进程不退**。**④ 原子落位**：校验通过的临时文件 `rename` 到 launcher 约定 pending 路径（运行二进制同目录 `beacon.new[.exe]`、同卷原子，[ADR-0045](docs/adr/0045-builtin-launcher-supervisor.md) 约定）。**⑤ 请求重启**：落位成功经进程内信号触发优雅关停，`cmd/beacon` 的 `run()` 返回 FR-96 已备的 `errRequestUpdateRestart` 哨兵 → `main()` 既有 `errors.Is` 以退出码 `70` 退出 → launcher 据约定换二进制重启（**FR-96 备好的退出出口本批接通**）。**⑥ 失败回滚**：任何阶段（查 Release / 下载 / 校验 / 落位）失败均保留旧二进制原样、进程不退、状态 `failed` 带原因（旧版继续服务）。**⑦ 进度内存态**（`idle`/`checking`/`downloading`/`verifying`/`staging`/`ready-restart`/`failed` + 百分比 + 目标版本 + 错误）线程安全、**不建数据库表**（瞬态走进程内存）。**⑧ 审计**：检查 / 应用 / 校验失败 / 落位完成各记一条（`internal/model/enums.go` 新增 `system.update-check`/`system.update-apply`/`system.update-failed` action 与 `system` TargetType，detail 含目标版本 / 结果摘要、不含敏感；前端 i18n 补三条中文映射 + #15 漂移守护清单同步）。被否方案：建 `update_history` 表（GitHub release + 审计已是真源）、引第三方 semver 库（窄格式自写足够）。**HTTP 触发入口（检查 / 状态 / 应用端点）属 FR-99**，本批提供 update 服务可调用方法（`CheckForUpdate`/`ApplyUpdate`/`Snapshot`）+ 退出接通，以 `httptest` mock release server **服务级测试**驱动「查 → 下载 → 校验 → 落位 → 请求重启」链路（含 semver 高/低/相等/rc 序/dev、SHA256 通过/失败、落位原子、failed 各分支、退出码交还）。**完整「真触发 → 换二进制 → launcher 重启 → 报新版」需 HTTP 触发（FR-99）+ Win/Linux 真机，待真机验。**
- 发布产物适配 launcher + 原生矩阵扩 linux-arm64（FR-102，feat，构建/发布/容器，无代码改动，依赖 FR-96、沿用 [ADR-0045](docs/adr/0045-builtin-launcher-supervisor.md)）：把 FR-96 新增的 launcher 监督进程 `beacon-launcher` 纳入正式发布产物与容器入口。**① Makefile**：新增 `launcher` 构建目标（与 `build` 并列，`-trimpath` + 注入**同一** `internal/version.Version`，产出 `dist/beacon-launcher[.exe]`；极薄不内嵌前端故不依赖 `web`），`package` 目标改为 `build launcher agent` 一并产出 launcher——本地 `make package` 现产 `beacon` + `beacon-launcher` + 双端 jar、三者版本恒一致。**② release.yml**：构建矩阵**新增 `linux-arm64`**（GitHub 原生 arm64 runner `ubuntu-24.04-arm`，仍 **CGO=1 原生构建、非交叉编译**，与文件头「sqlite 经 go-sqlite3 需 CGO」注释一致），共 **5 平台**（linux-amd64 / linux-arm64 / windows-amd64 / darwin-amd64 / darwin-arm64，**明确不含 windows-arm64**）；每平台原生构建并上传 launcher（命名 `beacon-launcher-<ver>-<target>[.exe]` 与主二进制对齐，上传 glob `beacon-*` 自然覆盖、`SHA256SUMS.txt` 一并校验）。**③ Dockerfile / compose**：构建阶段同时产出 `beacon` 与 `beacon-launcher` 同放 `/usr/local/bin`（launcher 按 `os.Executable()` 同目录定位 `beacon`），镜像 **`ENTRYPOINT` 由 `beacon` 切到 `beacon-launcher`**（与裸跑统一为「launcher 监督主进程」两形态，容器内崩溃自动重启）；compose 经 `build` 复用该镜像、无需改动。**裸跑兼容**：不经 launcher 直接跑 `beacon` 仍完全可用，仅退化为无自动重启（手动重启）。**容器在线更新形态**：容器内换二进制临时有效，镜像不可变、容器重建即丢更新，**容器形态生产升级一律以重拉镜像为准**（文档写明）。本批**不做** rc 预发布渠道与可复用 workflow 重构（留 FR-103），不动 tag 触发 glob，不写新 ADR（沿用 ADR-0045）。**CI 多平台产物（含 linux-arm64 原生 runner）待推 tag 真触发验证。**

### 变更
- 侧栏导航层级化（FR-93，增强 FR-6/FR-21，纯前端，无新依赖，见 [ADR-0043](docs/adr/0043-admin-nav-grouping-and-settings-aggregation.md) 与 [docs/specs/admin-nav-and-settings-ia.md](docs/specs/admin-nav-and-settings-ia.md)）：管理台侧栏 15 项平铺导航收为 **5 组可折叠手风琴**（概览 / 配置管理 / 集群 / 可观测 / 系统）。命中当前路由的组按 `location.pathname` 前缀**自动展开**；用户手动展开 / 折叠态**持久化到前端偏好**（`web/src/state/preferences.ts` 新增 `navExpandedGroups` 字段 + setter + 逐字段校验，剔除未知组 id / 非数组回落空数组）。折叠交互用**原生 `<details>/<summary>`**，不引 radix accordion 等新依赖。新增 `web/src/lib/navModel.ts` 作侧栏导航**单一真源**（5 组分组树 + 扁平叶子 + 命中组判定），Layout 树形分组渲染、CommandPalette 取扁平叶子，消除原 `Layout.NAV_ITEMS` 与命令面板导航目标两份重复清单的漂移。i18n 补 `nav.group*` 5 组标题（仅 zh-CN）。
- 设置区聚合页骨架（FR-94，增强 FR-62/FR-77，纯前端，沿用 [ADR-0043](docs/adr/0043-admin-nav-grouping-and-settings-aggregation.md)）：`/settings` 升级为**聚合页**——三块顶层 tab（**运维设置 / 系统信息 / 系统设置**），每块再含子 tab。顶层三块用**嵌套子路由**（`/settings` 重定向 `/settings/ops`；三块为 `/settings/ops`、`/settings/system-info`、`/settings/system-config`），块内子 tab 用 **search param**（如 `?tab=health`），满足深链 / 刷新保持 / 命令面板可检索 / 浏览器后退。运维设置 6 域（health/metric/longpoll/alert/log/reverse-fetch）改 **6 个子 tab 呈现**，但**保留跨子 tab 统一草稿 + dirty 计算 + 页脚批量保存 + 逐项恢复默认**（草稿态仍在块顶层集中持有、不下沉进单个子 tab，不回归 FR-62/FR-77）。系统信息块预留「版本与更新 / 控制面健康」、系统设置块预留「网络代理 / 更新设置 / API 密钥 / 环境管理」**空壳子 tab 容器**（占位文案，内容待 FR-95/99/100/101 填）。一屏不滚动版式：顶层 tab 栏 + 子 tab 栏常驻、运维设置批量保存条改块内 sticky 底栏、内容卡片化局部滚动。
- 旧页折叠进设置子 tab + 旧路由重定向（FR-95，增强 FR-82/FR-42/FR-53，纯前端，沿用 [ADR-0043](docs/adr/0043-admin-nav-grouping-and-settings-aggregation.md)）：把三个原独立页作为面板复用嵌入 FR-94 预留的子 tab 容器——控制面健康（原 `/system`，FR-82）并入**系统信息块 /「控制面健康」子 tab**，API 密钥（原 `/api-keys`，FR-42）并入**系统设置块 /「API 密钥」子 tab**，环境管理（原 `/namespaces`，FR-53）并入**系统设置块 /「环境管理」子 tab**。三页为自包含默认导出组件，直接作子 tab content 渲染；**降级其内部重复的页级 `<h1>` 标题**（子 tab 标签即标题），保留新建 / 刷新指示等内容入口。非激活子 tab 依赖 **Radix Tabs 默认卸载**（不 forceMount），使控制面健康页 5s 轮询切走时随卸载自然停。`App.tsx` 把 `/system`、`/api-keys`、`/namespaces` 三条旧路由改 `<Navigate>` 重定向到对应子 tab 深链（`/settings/system-info?tab=health`、`/settings/system-config?tab=api-keys`、`/settings/system-config?tab=namespaces`，与既有 `/instances`→`/servers` 重定向风格一致）。`navModel`（侧栏 + 命令面板单一真源）的「系统」组补这三条子 tab 深链叶子，保证命令面板搜「密钥 / 环境 / 控制面健康」可达、侧栏可点达。

## 0.14.1（2026-06-25）

### 修复
- 批量端点加 ids 上限并修批量软删 / 置态的并发幽灵审计（修复 FR-74）：`POST /admin/v1/{configs,files}/batch` 的 `ids` 此前无数量上限，现加护栏 `MaxBatchIDs=2000`（与单次导入 `MaxImportFiles` 同口径），超限 `400 INVALID_PARAM`；批量内部此前逐项 `Get`（N+1），现去重后**一次** `WHERE id IN (?)` 批量预取（占位符无方言、可移植），预取数 < 去重 id 数即整批 `404`。同时修 TOCTOU 幽灵审计：repo `SoftDelete` / `SetEnabled`（config + file 两边）此前忽略 `RowsAffected`、命中 0 行仍返 nil——并发场景下预取通过但事务内目标已被另一请求软删时，会写一条「实际未发生」的审计且不回滚；现校验 `RowsAffected`、0 命中返 not-found，由 `batchMutate` 触发整批回滚（不提交、不写任何审计）。预取批量存在性 + 事务内 `RowsAffected` 双保险。末轮评审控制面 follow-up。
- 补 `instance.tail-logs` 审计动作中文映射 + 漂移守护清单（修复 FR-88 / #15）：FR-88「查看 agent 日志」每次会落一条 `instance.tail-logs` 审计，但 `web/src/i18n/locales/zh-CN.ts` 的 `audit.action` 缺该映射、且 #15 漂移守护清单（`auditActionCoverage.test.ts` 手工镜像 `enums.go`）也漏镜像该枚举——守护测试因此全绿却放行，审计日志页（FR-7）/ 服务分析页（FR-73）「动作」列对该动作显原始英文 `instance.tail-logs`。补中文映射「查看 agent 日志」并把 `instance.tail-logs` 纳入守护清单（守护用例 51→52）。运维体验优化程序末轮总评（前端 reviewer）发现；守护清单为手工镜像、仍存同类漂移风险，自动派生留作后续改进。
- 编辑器格式校验改 useMemo + 去抖，消除大 YAML 每击键卡顿（修复 FR-75）：`CodeEditor` 此前每次 render 同步跑一次完整 `js-yaml` 解析，编辑大 YAML 时每击键阻塞主线程。改为对去抖（250ms）后的内容用 `useMemo` 校验——编辑器内容仍即时显示、错误条延迟出现；去抖未落定窗口保守视为「校验中」、保存保持禁用，确保非法内容不漏过保存。末轮评审前端 follow-up。
- 批量导出改 `allSettled` 容部分失败 + 全选半选态（修复 FR-74）：配置批量导出此前用 `Promise.all`，任一项拉取失败即整体抛错、已成功项全丢、无文件下载；改为 `Promise.allSettled`，成功子集照常打包下载并对失败项提示「N 项导出失败」，仅全失败才不下载并报错。同时修表头全选框：部分选中时显 `indeterminate` 半选态（此前仅全选才勾选，与「已选 K 项」文案矛盾）。导出 / 审计导出的 `revokeObjectURL` 改 `setTimeout(…, 0)` 延后释放更稳。末轮评审前端 follow-up。
- agent 强制重同步在停机窗口诚实回传（修复 FR-91）：`AgentLifecycle.forceResyncNow` 改返回执行结果——当 agent 未运行 / 正在停机（resync 命令恰在 `shutdown` 置 `running=false` 后、命令 async 流仍在跑的极窄窗口）跳过重拉时，此前 `runResync` 仍回传 `ok=true` 误把命令标 `done`；现回传 `failed`（reason「agent 未运行/正在停机，跳过强制重同步」），控制面 CAS 转 `failed`、不再误报完成。末轮评审 agent follow-up。

## 0.14.0（2026-06-25）

### 新增
- 暗色模式 / NOC 大屏只读 / 紧凑密度（FR-92，feat，纯前端，无新 ADR / 无新依赖，见 [docs/specs/theme-wallboard-density.md](docs/specs/theme-wallboard-density.md)）：给管理台补三项纯前端呈现选项并**持久化偏好**。新增偏好 store `web/src/state/preferences.ts`（镜像 `state/auth.ts` 的 `useSyncExternalStore` + 监听者广播，`localStorage` 键 `beacon.preferences` 存 `{theme, density}`，读写 try/catch 兜隐私模式、非法值逐字段回落默认；**不引入 `next-themes` 避免双真源**），作为主题 / 密度的单一真源。**① 暗色主题**：页眉太阳 / 月亮按钮浅 ↔ 暗一键切换，复用 `index.css` 既有 `.dark` 变量；`main.tsx` 渲染前同步按持久化值给 `documentElement` 打 `.dark` 类避免首屏闪烁，`App.tsx` 订阅 store 运行期同步。**② 紧凑密度**：页眉密度开关（舒适 ↔ 紧凑），`DataTable` 直接读偏好——紧凑档收紧表头高度（`h-8`）与单元格内边距（`py-1`），所有走统一表格的列表页自动响应。**③ NOC 大屏只读看板**：新路由 `/wallboard`（极简无侧栏 `WallboardLayout` 承载 `WallboardPage`，页眉仅含退出大屏 + 主题切换），复用可观测看板的只读 `metricsSummary` / `listInstances` 与 `SummaryCards` / `BCPanel` / `StatCard` 组件，大字号大间距全屏呈现，**纯只读、不含任何下线 / drain / 改派 / 编辑 / 筛选入口**，零新增后端端点。页眉 `HeaderControls`（主题 / 密度切换 + 大屏入口，挂进 `SystemHeader`）复用既有 `lucide-react` 图标、未引入新图标 / 主题依赖。新增 `preferences.*` / `wallboard.*` 中文文案（仅 zh-CN）。
- 服务器行快捷操作 + 强制重同步（FR-91，增强 FR-65，全栈含 agent，复用 [ADR-0027](docs/adr/0027-reverse-fetch-channel-and-security.md) 命令队列、未引入新 ADR，见 [docs/specs/server-row-quick-actions.md](docs/specs/server-row-quick-actions.md)）：服务器页行操作收进单个「⋯」下拉菜单（保留下线 FR-76 二次确认 / drain·undrain / 改派），并补三入口——**agent 详情**（打开服务器详情 Sheet）、**查看日志**（打开详情 Sheet 并直达其日志区、复用 FR-88）、**强制重同步**。强制重同步令在线 agent 立即重拉控制面权威的有效配置/文件树/覆盖集并 apply（免等长轮询、免上机），是 `agent_command` 命令队列既有模式的**第三个命令类型**（`resync-config`，与 `ingest-plugins`/`tail-logs` 并列、空载荷、无内容回传）。**控制面**：`AgentCommandService.RequestResync` 事务内建 pending 命令 + `instance.resync` 审计（detail 仅 commandId/serverId）+ 唤醒 agent；新端点 `POST /beacon/v1/agent/commands/result` 收 agent 回传、`ReceiveResyncResult` CAS `fetched→done`（成功）/`failed`（失败）；admin 触发端点 `POST /admin/v1/instances/{serverId}/resync`（写 readonly→403，专项审计纳入 coveredWriteRoutes）。**agent-core**：`AgentLifecycle.forceResyncNow()` 依次复用既有 `fetchAndApply{Config,FileTree,Override}Once` 三条「以本地 md5 拉一次 → applier 幂等 apply」路径（已是最新则合法 no-op、**不读 plugins 树**、不做绕过幂等守卫的 force-apply）；`ReverseFetchExecutor` 加 `resync-config` 分支调注入回调并经 `uploadCommandResult` 回传；装配以延迟持有者打破 executor 先于 lifecycle 的构造顺序。**前端**：`triggerResync` + ServersPage 下拉菜单（下线确认弹窗提到菜单外层受控触发以保二次确认）+ ServerDetailSheet `focusLogs` 入口直达日志区。**改 agent-core → 双端 jar 重建重部**。

## 0.13.0（2026-06-25）

### 新增
- agent 版本/构建可见性（FR-86，增强 FR-34/FR-52，全栈含 agent，见 [ADR-0039](docs/adr/0039-agent-self-reported-version.md) 与 [docs/specs/agent-version-visibility.md](docs/specs/agent-version-visibility.md)）：治「装了过期 agent jar 却在 UI 不可见」的运维盲区。agent 注册时**自报自身构建版本**——注册 payload 新增可选 `agentVersion`（agent-core `AgentIdentity` 加字段，壳层 bukkit/bungee 经 TabooLib `pluginVersion` 注入，core 不碰平台/具体库守 [ADR-0005](docs/adr/0005-agent-transport-codec-abstraction.md)；与业务 `version` 语义不同，仅非空时拼键、旧 agent 缺键向后兼容）；控制面注册端点解析后存入内存注册表 `Instance.AgentVersion`、`InstanceView` 暴露 `agentVersion`（旧 agent 为空串），只读、不落 DB、不参与决策。管理台服务器页新增「agent 版本」列与单服详情字段，并按**环境（namespace）** 聚合多数版本、对版本与多数不同的实例打**黄标**提示运维去对齐（多数版本判定为纯前端展示派生 `lib/agentVersionConsistency.ts`，穷举单测；空版本显「未知」不打标）。双端 agent jar 需重建重部署才显真实版本——未升级者显「未知」正是本需求要暴露的信号。
- 反向抓取受管任务进度 + 错误回传（FR-87，增强 FR-58/FR-60，全栈含 agent，扩展 [ADR-0037](docs/adr/0037-reverse-fetch-managed-task.md) spec、无新 ADR，见 [docs/specs/reverse-fetch-task-progress.md](docs/specs/reverse-fetch-task-progress.md)）：治受管任务「卡在 scanning/fetching 不动、UI 无进度 / 无超时 / 无错误，只能翻磁盘日志才定位」的盲区。**① 进度·已用时长**：任务视图新增派生 `elapsedSec`（控制面渲染时刻 UTC 距 `updatedAt` 的秒数、≥0、不落库），管理台任务台显「已用时长」列，非终态停留超前端阈值（`STUCK_THRESHOLD_SEC=120s`，纯函数 `lib/reverseFetchProgress.ts` 穷举单测）的任务标**「疑似 agent 未响应」**警示。**② 错误回传**：agent 执行 scan/submit **读盘失败**时（此前只 agent 本地日志后静默放弃），经**新端点** `POST /beacon/v1/agent/files/error`（`{commandId, reason}`、agentToken 信任面、`BeaconApiClient.uploadError` 经适配器层守 [ADR-0005](docs/adr/0005-agent-transport-codec-abstraction.md)、async 不碰主线程、best-effort 不重试）主动回传错误；控制面据 commandId 反查所属任务（scan→`scanning` / submit→`fetching`），CAS 转既有终态 `failed`、写新字段 `last_error`、命令转 `failed`、清瞬态、解除互斥、记 `file.reverse-fetch-error` 审计（detail 不含文件内容），命令 / 任务态不符按 404/409 拒（幂等、不误终结无关任务）；管理台 `failed` 任务展示 `lastError`。`reverse_fetch_task` 加 `last_error` `VARCHAR` 列（可移植、与 `note` 结果摘要分立）。**改 agent-core → 双端 jar 重建重部**。
- 在线日志/诊断查看器（FR-88，feat，全栈含 agent，见 [ADR-0040](docs/adr/0040-agent-readonly-log-tail.md) 与 [docs/specs/agent-log-viewer.md](docs/specs/agent-log-viewer.md)）：治「agent 日志只在磁盘、排障必须上机器」的盲区，让运维在管理台「服务器详情」直接拉某在线 agent 的最近 N 行**自身**日志（脱敏后）。agent 是插件、无 HTTP server，故复用 [ADR-0027](docs/adr/0027-reverse-fetch-channel-and-security.md)/[ADR-0037](docs/adr/0037-reverse-fetch-managed-task.md) 的「命令-回传」周期（不给 agent 开端口、控制面不直连）。**agent-core**：`BufferingPlatformAdapter` 包裹壳层 adapter（壳日志零改动），所有 `info/warn/error` 旁路进线程安全、有界（默认 300 行）的 `AgentLogBuffer` 内存环形缓冲，**落缓冲那一刻即经 `LogRedactor` 脱敏**（token/password/secret/authorization/bootstrap-token 等键值掩码，纯函数穷举单测）；收到 `tail-logs` 命令即读缓冲快照经新端点 `POST /beacon/v1/agent/logs` 回传——**绝不读任何磁盘日志文件**，读快照 + HTTP 仍只在 async 线程。**控制面**：`agent_command` 加 `tail-logs` 类型与瞬态列 `log_content`；`AgentLogService` 编排触发（**单活跃限速**：进行中再触发→`409 AGENT_LOG_ACTIVE`，建 pending 命令 + `instance.tail-logs` 审计 detail 不含日志内容、唤醒 agent）/ 回传转存（CAS done）/ 查询；瞬态取一次后随命令过期清空、不落持久真源、不进审计 detail、不导出。admin 端点 `POST/GET /admin/v1/instances/{serverId}/logs`（触发写 readonly→403 + 查询读，从无命令→204）。**前端** 服务器详情 Sheet `LogsSection`：触发→轮询命令状态→展示脱敏日志（按级别着色）。**安全边界（ADR-0040）**：只 agent 自身日志、不读任意文件、落缓冲即脱敏、行数有界、限速、agentToken 信任面——即便控制面被攻破也只能看到 agent 自己脱敏后的近期日志。**改 agent-core → 双端 jar 重建重部**。
- 告警历史 / 事件信息流（FR-89，feat，控制面 + 前端无 agent 改动，见 [ADR-0041](docs/adr/0041-alert-event-persistence.md) 与 [docs/specs/alert-event-feed.md](docs/specs/alert-event-feed.md)）：治「FR-28 告警只往外推 webhook / 站内信、控制台无任何告警历史 / 信息流」的盲区。新增 `alert_event` **持久化实体**（`type`/`level` 落 `VARCHAR`+应用层校验、`detail` 落 `TEXT`、`created_at` UTC，可移植 GORM、`store.Open` AutoMigrate 注册）记录告警事件，与 FR-28 既有通道**并存**——通道是「往外推 / 进程内即时」，本表是「留痕 + UI 信息流」跨重启可查。落库接法是**新增一个 `PersistAlerter` 告警通道**（实现 ADR-0019 的 `Alerter`、经窄 `EventSink` 接口依赖 service 守循环依赖红线），复用既有 `Dispatcher` 扇出——健康流转触发点零改动即自动留痕，落库 IO 在注册表锁外、失败仅 WARN 不阻断扫描与其它通道。新增只读端点 `GET /admin/v1/alert-events?type=&level=&namespace=&from=&to=&page=&size=`（分页 + 过滤、时间倒序，分层 `router→handler→service→repository`、handler 不碰 GORM、仅占位符无方言）。**前端**新增「事件」页（`/alert-events`，侧栏导航）：信息流时间线（级别着色圆点 + Badge）+ 按类型 / 级别 / 环境 / 时间过滤 + 分页 + 详情弹窗。区别于审计日志（人对平台的操作）与负载看板（运行时负载），本页是系统健康事件历史留痕。范围纪律：只做「持久化既有告警事件 + 列表 + UI 信息流」，不新增告警规则 / 不改 FR-28 通道行为 / 不做告警确认工作流（ADR-0041 的「告警不落库」取代 ADR-0019 同款结论）。`publish-fail`/`backend-unreachable` 仅预置为类型枚举取值、当前无触发点（不凭空制造）。
- 脚本化 admin API token + 复制为 curl（FR-90，feat，纯前端，见 [ADR-0042](docs/adr/0042-admin-api-token.md) 与 [docs/specs/admin-api-token.md](docs/specs/admin-api-token.md)）：治「运维走 API/curl 自动化但缺上手辅助」的盲区。盘既有鉴权面确认 FR-42（[ADR-0026](docs/adr/0026-runtime-api-keys-and-readonly-role.md)）的 `bk_` 运行时 API 密钥**已是**脚本化 admin token——库内只存 SHA-256 哈希、可吊销（软删）、可到期（`expiresAt`，过期即 401）、`full`/`readonly` 角色（readonly 写端点一律 403）、`X-Beacon-Api-Key`/`Bearer` 调 `/admin/v1`、创建/吊销/重置入审计，区别于 agent 数据面共享 `X-Beacon-Token`（仅防误连）。故 FR-90 不新增并行凭据、零后端改动，**增量仅为前端「复制为 curl」自动化辅助**：在一次性明文展示弹窗增「复制为 curl」按钮，复制一条带认证头、指向只读样例端点 `GET /admin/v1/system/status` 的可粘贴命令。命令拼接为纯函数 `lib/curlCommand.ts`（含单引号 shell 安全转义，穷举单测），token 仅在浏览器内存拼接、不落库 / 不入日志。范围纪律：不做 OAuth/SSO/细粒度 ACL/多租户（ADR-0026 既定不做项）。

## 0.12.0（2026-06-24）

### 新增
- 健康流转原因展示（FR-81，增强健康，见 [docs/specs/health-status-reason.md](docs/specs/health-status-reason.md)）：实例视图（`GET /admin/v1/instances`、`.../instances/{serverId}`）新增 `lastHeartbeatAgeSec`（距上次心跳秒数，按控制面渲染时刻 UTC 算、负值归零）与 `healthReason`（触发当前状态的原因文案，如「35s 未心跳 > ttl 30s」；degraded/lost/offline 各显对应阈值、online 空串；阈值取当前生效健康阈值，与健康扫描判定同源，纯内存派生不落 DB）；管理台服务器页 / 看板 / 单服详情的状态徽标在原因非空时以悬浮提示展示，让运维一眼看懂「为何是这个状态」。原因文案纯函数 `runtime.HealthReason` 与状态机 `healthByAge` 同组阈值口径一致。
- 配置发布影响面预览（FR-79，增强 FR-22，见 [docs/specs/publish-impact-preview.md](docs/specs/publish-impact-preview.md)）：发布确认对话框新增「将影响 N 台在线服：serverId…」一行，治运维点保存时看不到本次改动会落到哪些在线子服、易误发到比预期更大范围的问题。后端新增只读端点 `GET /admin/v1/configs/impact?namespace=&scopeLevel=&group=&scopeTarget=`，按 `zone_assignment`（DB 权威归属，[ADR-0004](docs/adr/0004-zone-authority-control-plane.md)）解出该 scope 覆盖的子服集合、与内存注册表**可用集合**（online+degraded，与发现 / 拓扑 / 长轮询同口径，degraded 仍会收到变更）求交，返回 `{ namespace, scopeLevel, group, scopeTarget, affected: [serverId...], total }`（serverId 字典序）。覆盖语义与有效配置覆盖链对称（global 全量 / group 按大区 / zone 按大区+小区 / server 按 serverId，未指派回退 groupHint），与 agent 端 `Resolve` 同口径；一次拉全环境归属避免 N+1，纯读不落 DB、不参与发布决策。分层不破（handler 不碰 GORM / 注册表内部结构），可移植 GORM（仅复用既有归属查询、无方言）；前端对话框打开时拉端点、加载 / 0 台 / N 台三态纯展示、不阻断发布。
- 新服接入引导向导（FR-85，feat，纯前端，见 [docs/specs/server-onboarding-wizard.md](docs/specs/server-onboarding-wizard.md)）：服务器页加「添加服务器」分步向导——① 填环境 / serverId / 角色（bukkit·bungee）/ 大区 / 地址，进入下一步前按目标环境拉 `listInstances` 校验 serverId 不重复（撞名拦截并提示，早期反馈、真源唯一性仍由控制面注册把关）；② 按所填值生成可一键复制的 **agent `config.yml` identity 段**（含中文注释、显式不含 zone）+ **run 脚本 env 片段**（`BEACON_AGENT_IDENTITY_NAMESPACE/SERVER_ID/GROUP_HINT/ADDRESS`，FR-41 覆盖、优先于本地 yaml）；③ 可选预建 zone 指派（仅 bukkit，复用既有 `PUT /admin/v1/zones/assignments`，使新服上线即归属正确小区、不经「未分配」中间态；BC 代理不进 zone 指派、不展示该入口，与后端校验一致）。片段拼接为纯函数 `lib/agentOnboarding.ts`。**纯前端**、复用既有端点（查重 `listInstances`、指派 `assignZone`、环境下拉 `listNamespaces`），未新增后端 / 未改契约 / 未引入依赖。
- 审计全文检索 + 导出（FR-84，增强 FR-7，全栈，见 [docs/specs/audit-search-export.md](docs/specs/audit-search-export.md)）：审计页新增「详情关键字」检索框（`GET /admin/v1/audits` 加 `detailKeyword` 参数，对 `detail` 列做子串 LIKE 检索、与既有过滤 AND 叠加，`%`/`_` 转义当字面字符）+「导出 CSV / 导出 JSON」按钮；新增 `GET /admin/v1/audits/export?<同 audits 过滤>&format=csv|json`，复用 List 全部过滤（含 detailKeyword、不分页全量），按时间倒序**流式**输出（控制面 `repo.Stream` 游标分批边查边写、不一次性载入内存），CSV 带表头、JSON 为记录数组，均附 `Content-Disposition`；非法 format 写头前 `400 INVALID_PARAM`。分层不破（handler 不碰 GORM），LIKE 用占位符 + `ESCAPE ?`（转义字符避开反斜杠以兼容 MySQL/Postgres，集成测试逮出并修复方言 SQL 语法错），保可移植；前端导出走 `fetch` 带令牌取 Blob 下载、令牌不入 URL。
- per-server 有效配置变更时间线（FR-80，feat，全栈，见 [docs/specs/server-config-timeline.md](docs/specs/server-config-timeline.md)）：服务器详情 Sheet 新增「变更历史」区，回答「这台服的有效配置何时因哪次发布变过」。后端新增只读端点 `GET /admin/v1/instances/{serverId}/config-timeline?namespace=&group=`，按 `zone_assignment`（DB 权威，[ADR-0004](docs/adr/0004-zone-authority-control-plane.md)）解出该服当前 `(group, zone)`、未指派回退 groupHint，取其四层（global/group/zone/server）覆盖链涉及的 config 项后**一次按 itemID 集合拉全部 `config_revision`**（避免 N+1），每条标注其所属项的 scope，按 `createdAt` 倒序汇总，返回 `{ namespace, serverId, group, zone, items: [{ configItemId, dataId, scopeLevel, scopeTarget, version, md5, operator, comment, createdAt }] }`（含首发 / 发布 / 回滚，只读、不含 content、不落 DB）。分层 `router → handler → service(EffectiveService) → repository`（handler 不碰 GORM），可移植 GORM（`IN (?)` 占位 + 既有归属 / 候选查询、无方言）；前端 `ServerDetailSheet` 打开时拉时间线，列每条（dataId · 覆盖层中文 · v版本 · 时间 · 操作者 · 说明），加载 / 失败 / 空 / 列表四态。覆盖链按**当前**归属解析、不回溯历史区段；当前不分页。
- 控制面自观测页（FR-82，feat，全栈，见 [docs/specs/control-plane-self-observability.md](docs/specs/control-plane-self-observability.md)）：新「控制面健康」页（`/system`，侧栏导航项）+ 只读端点 `GET /admin/v1/system/observability`，补 FR-33 页眉条之外的控制面**进程内部运行态**——DB 连接池统计（`sql.DBStats`：上限 / 已建 / 使用中 / 空闲 / 累计等待次数 + 时长，database/sql 通用非方言）、长轮询四通道（配置 / 文件 / 拓扑 / 命令）当前挂起 waiter 数 + 合计、注册表规模（实例总数 + 按健康状态分布）、命令队列深度（`agent_command` 按状态计数，一次 GROUP BY）。回答「Beacon 自己卡不卡、连接池有没有耗尽、长轮询挂了多少、命令队列堆没堆」，与 FR-32 MC 负载看板 / FR-73 服务分析清晰区分（本页是控制面自身健康、只读）。**选新端点而非扩 FR-33 `system/status`**：status 被每页页眉 5s 高频轮询（含 FR-78 连通态派生），不宜把连接池 / 注册表遍历 / 命令队列 DB 计数压进去；自观测页只在打开时轮询。分层 `router → handler(ObservabilityHandler) → service(ObservabilityService) → registry/repository`（handler 不碰 GORM / 注册表内部结构，经只读快照），新建独立 service 不塞进 SystemService（避免上帝类）；新增 `Registry.StatusCounts()`（持读锁一次计数）与 `AgentCommandRepository.CountByStatus()`（可移植 GORM、无方言）；命令计数失败优雅降级为空、不阻断其余指标。StatCard 网格四组分区展示、短周期轮询刷新。
- 全局搜索 + 命令面板（FR-83，feat，纯前端，见 [docs/specs/command-palette.md](docs/specs/command-palette.md)）：管理台加全局命令面板——`Ctrl/Cmd+K` 任意页面唤起（侧栏亦有「搜索…」入口、提示 `Ctrl K`），单输入框即时聚合搜索、纯键盘可达（方向键上下选 / 回车跳转 / Esc 关）。结果按四组展示并深链跳转：**导航**（所有页面 → 对应路由，复用既有 `nav.*` 译名）、**配置 / 文件**（按 `dataId` / `path` 命中 → `/configs?dataId=` / `?path=`）、**服务器**（按 `serverId` 命中 → `/servers?serverId=`）、**审计动作**（高频动作快捷项 → `/audits?action=`）。空 query 只显导航 + 审计动作（避免一开就刷全量噪声），有输入才按子串（大小写无关、匹配 title+subtitle）过滤命中数据。搜索逻辑抽为纯函数 `lib/commandPalette.ts`（buildItems / filterItems / groupItems，穷举单测）；面板打开时一次性拉 `listConfigs/listFiles/listInstances`（无 namespace = 全部）客户端过滤，**拉取失败降级为只剩导航 + 审计动作、面板不被阻断**。**纯前端**，复用既有列表端点、未加后端 / 未改契约、**未引入 `cmdk` 依赖**（用既有 `radix-ui` Dialog 基元 + 自实现键盘导航）。深链查询参数为「尽力定位」，目标页当前不消费亦能正确导航、不报错。

### 修复
- 破坏性确认框复述比对忽略首尾空白（修复 FR-76）：高摩擦档手输复述用严格 `===` 比对，当 `confirmPhrase` 取的名称（如 API 密钥名）带前后空格时，用户照抄可见正文（无空格）也无法启用确认，导致吊销 / 删除被卡死。改为两侧 `trim()` 后比对（仍区分大小写以保留摩擦强度）；补该场景回归用例。v0.11.0 代码评审发现。

## 0.11.0（2026-06-24）

### 新增
- 运维设置页恢复默认 + 批量保存（FR-77，增强 FR-62，纯前端，见 [docs/specs/settings-page-enhance.md](docs/specs/settings-page-enhance.md)）：FR-62 运维设置页此前只能逐项编辑、逐项保存。现叠加两处批量体验——① 每项加「恢复默认」按钮（把该项编辑值一键置回其默认值，草稿已在默认值时禁用，仅改本地草稿、仍走保存落库）；② 页脚「保存全部变更（N）」把所有「已改未存」项一次性逐个 PUT（前端循环复用 FR-61 既有 `PUT /admin/v1/settings/{key}`，单项失败不阻断其余，完成后刷新列表回显热生效值并出「成功 M / 失败 K」汇总提示）；③ 顶部与页脚「改动摘要」逐脏项列出「key：旧值 → 新值」。草稿集中上提至页顶统一持有（列表刷新后仅同步非脏项、保留正在编辑的脏草稿）。**纯前端**，未加后端端点 / 未引入批量原子接口、未改 API 契约（部分成功是预期语义）。
- 控制面连接状态指示 + 自动重连（FR-78，feat，纯前端，见 [docs/specs/connection-status-indicator.md](docs/specs/connection-status-indicator.md)）：管理台全局加连接状态小灯（页眉品牌旁，绿=已连接 / 红=已断开 / 灰=连接中），控制面轮询请求失败时主内容列顶部弹横幅「控制面连接中断，正在重连…」、恢复后横幅自动消失并触发一次全量 `invalidateQueries` 刷新各页数据，治控制面重部（重启 beacon-cp）时 UI 静默掉线、须手动刷新才发现的问题。**连通态由既有 `system-status` 轮询查询的成功 / 失败派生**（管理台无面向浏览器的 SSE，唯一 SSE 是 agent↔控制面、不面向浏览器；新 `useConnectionStatus` hook 复用 `SystemHeader` 的 5s 心跳轮询、不另起传输通道 / 后端端点）。纯前端、未改后端 / 契约 / 依赖。
- 配置编辑器客户端格式校验（FR-75，增强 FR-1/FR-3，纯前端，见 [docs/specs/config-editor-lint.md](docs/specs/config-editor-lint.md)）：配置中心在页编辑此前不做任何客户端格式校验，语法非法（YAML Tab 缩进 / 引号·括号未闭合、JSON 多逗号 / 缺引号）的内容可一路点到「确认保存」、直到落到控制面 FR-27 服务端校验甚至下发 agent 才暴露。现给 Monaco 编辑器接**客户端格式校验**（纯函数 `lib/configLint.ts`）：`format=json` 用 `JSON.parse` 并尽力把错误位置映射到行号；`format=yaml` 用 **js-yaml** 做完整 YAML 1.2 解析（锚点 / 别名 / 多文档 / 深层流式集合皆覆盖，解析异常取 `mark.line` + `reason` 定位）；properties / plaintext 与空内容放行。解析失败时编辑器旁出**行内错误条**（行号 + 信息）、**保存按钮禁用**并拦 Ctrl+S 唤起的保存，合法才可发布。与 FR-27 服务端 schema 校验**互补不替代**（前端可绕，服务端仍是权威）；**纯前端**、未改后端 / 契约；引入 `js-yaml` 依赖（经用户确认）。
- 配置 / 文件批量操作（FR-74，增强 FR-38/FR-1，见 [docs/specs/config-file-bulk-ops.md](docs/specs/config-file-bulk-ops.md)）：治「逐个删」低效——配置中心列表加多选 + 批量操作栏（删除 / 禁用 / 启用 / 导出），空选禁用按钮、批量删除前轻量二次确认（FR-74 自带、不依赖 FR-76），导出为前端逐项拉选中内容打包 JSON 后 Blob 下载（best-effort、无新依赖）。后端新增批量端点 `POST /admin/v1/configs/batch`、`POST /admin/v1/files/batch`（`{action: delete|disable|enable, ids}`），把一组配置项 / 文件对象的删除 / 禁用（enabled=false）/ 启用（enabled=true）在**一个事务**内原子完成、每项各记一条领域审计（`config.disable/enable`、`file.disable/enable` 等），任一 id 不存在则整批回滚、提交后逐项唤醒长轮询；空 ids / 非法 action 400。分层不破（handler 不碰 GORM），MySQL 集成验批量事务可移植。

### 变更
- 破坏性写操作统一二次确认（FR-76，增强 FR-67，纯前端，见 [docs/specs/destructive-confirm.md](docs/specs/destructive-confirm.md)）：抽通用确认组件 `DestructiveConfirmDialog`（复用 `ui/alert-dialog`）——标题 + 破坏性动作描述 + **影响摘要**（脱链哪层 / 影响哪些服，调用方按上下文传入）+ 确认 / 取消，并提供可选「需输入复述」高摩擦档（参考 FR-71 改派手输 serverId）。接入 FR-74 不触碰的破坏性入口：环境删除（`NamespacesPage`，带影响摘要）、API 密钥重置（`ApiKeysPage`，带影响摘要）、API 密钥吊销（`ApiKeysPage`，带影响摘要 + 手输密钥名复述高摩擦档）；各入口确认才执行、取消不执行，消除原先各页手搓 `AlertDialog` 的复制粘贴。删配置 / 删文件 / 清小区默认入口当前无 UI 写入口故本期不接（待入口落地再纳入）。**纯前端**，未改后端 / API / 契约；后端守卫（环境删除 409、密钥软删）仍是唯一安全边界。

### 修复
- 补全审计动作中文映射并加 i18n 漂移守护测试（修复 FR-7/FR-73）：审计日志页 / 服务分析页的「动作」列对部分 `action` 枚举显原始英文 key——FR-74 新增的 `config/file.disable/enable`、以及历史遗留的 `override-set.*`、`reverse-fetch.ignore-rule-*`、`settings.update` 共 11 个 action 缺中文映射。补全 11 条；新增前端守护测试 `auditActionCoverage.test.ts`：以镜像 `internal/model/enums.go` 的 action 清单逐一断言每个 action 都有非空 `audit.action` 中文映射（并查无清单外多余 key），思路同后端 `coveredWriteRoutes` 漂移守护，从此「加了审计动作忘配中文」会被测试挡下。
- 控制面 GORM 时间戳统一 UTC（修复 FR-73 服务分析默认窗口漏掉最近活动）：GORM 默认 `NowFunc` 用 `time.Now()`（本地时区），在非 UTC 时区机器上 `autoCreateTime` 把 `audit_log.created_at` 等写成带本地偏移的时间（如 +08:00），而注册/健康等内存侧时间一律 UTC、FR-73 服务分析按 UTC 时间窗过滤审计——本地时间戳在 sqlite 字符串比较下被当成「未来若干小时」，使默认「近 30 天」窗口把最近约「时区偏移」小时内的活动错误排除（+08:00 真机默认视图丢最近约 8 小时）。`store.Open` 的 `gorm.Config` 设 `NowFunc` 恒返回 UTC，全表自动时间戳统一 UTC、与系统其余部分一致；补 store 层回归测试（强制本地时区 +08:00 建记录、断言 `CreatedAt` 为 UTC）。真机多服集群验收时暴露。

## 0.10.0（2026-06-24）

### 新增
- 写操作审计中间件兜底（FR-72，增强 FR-7，见 [docs/specs/audit-write-middleware.md](docs/specs/audit-write-middleware.md)）：在 `/admin/v1` 鉴权与只读闸之后挂一道审计中间件，对所有写方法（POST/PUT/DELETE）兜底记审计（操作者 + 动作 + 目标 + 结果 + 来源 IP，动作 / 目标由 chi `RoutePattern` 与 method 推导，**敏感内容不入 detail**）。语义为 **gap-filling**：仅对尚无专项审计的写端点补记，以集中常量清单标注已被既有 14 类专项审计覆盖的路由、命中即跳过，**不与既有专项审计重复双记**；既有专项审计全部保留。当前存量写端点均已带专项审计，故落地后对存量端点零新增审计行，价值在于对**未来新增写端点的结构性兜底**（新增「自带专项审计」的端点须同步登记进覆盖清单，否则会被弱重复补记一条）。审计落库失败仅 WARN、绝不阻断主响应。
- 运维设置 store + 热生效（FR-61，feat，[ADR-0038](docs/adr/0038-ops-settings-store-hot-reload.md) 扩展 FR-25「config.yml 即真源」，见 [docs/specs/ops-settings-store.md](specs/ops-settings-store.md)）：控制面热改项的真源从 config.yml（启动期一次性读、改任何项都得重启）移到 DB 设置 store，运维改参免重启。新 `setting` key-value 单表（可移植 VARCHAR + `value_type` + 乐观锁，**列名 `setting_key`** 避开 MySQL 保留字 `key`）+ 设置 API（`GET /admin/v1/settings` 只读列全部热改项、`PUT /admin/v1/settings/{key}` full 角色改 + 审计 `settings.update`，非法值 / 白名单外 key 拒、readonly 403）。**热改项**（健康亚健康 / TTL / 离线宽限 / 扫描间隔、采样开关 / 间隔 / 保留、长轮询挂起上限、告警 webhook url / 超时、日志级别、反向抓取单文件上限）由各消费者**运行期从设置 service 读、热生效**（设置 service 持内存缓存 + Update 即刷新；健康 / 采样 ticker 变即重置；日志级别经 atomic-level slog handler 即时改；反向抓取 `overThreshold` 按设置 + agent 上报 size 重算，不改 agent），无观察者 / 事件总线（守"禁重型件"）。config.yml 对热改项**降为引导 + 首启 seed**（store 无该 key 才填，已 seed 后改 config.yml 不影响运行值）。**启动 / 安全项（http-addr、database.\*、auth 口令 / 签名密钥、agent-token、git-export 路径）铁留文件**——是安全边界，绝不进 store / API / UI、登录后不可见、不可读。
- 运维设置页（FR-62，feat 依赖 FR-61，纯前端）：新增管理台「运维设置」页（`/settings`），`GET /admin/v1/settings` 拉热改项按域分组（健康检查 / 指标 / 长轮询 / 告警 / 日志 / 反向抓取）展示每项中文说明 + 当前值 + 默认值，按类型逐项编辑（`int` 数字 / `bool` 开关 / `string` 文本，`log.level` 下拉枚举 ERROR/WARN/INFO/DEBUG），`PUT /admin/v1/settings/{key}` 逐项保存（值未变禁用、成功提示热生效、后端非法值 400 回显），并明示**启动 / 安全项在 config.yml、此处不可改不可见**。消费 FR-61 端点、未改后端。

### 变更
- 拓印审核台以关键字搜文件替代手输路径（FR-69，增强 FR-46，纯前端）：拓印触发此前需手输目标文件相对路径，易错且大树难定位。现按所选在线源实例的有效文件树（`GET /admin/v1/files/effective`）拉候选、复用可编辑下拉框做**客户端即时关键字过滤**（不每键打后端、大树不卡），并加使用提示；**保留手输自定义 path 兜底**，对未被 Beacon 追踪的文件仍可拓印。未新建后端端点、未改 API 契约。
- 启用环境名称并使各筛选下拉显示「编码 · 名称」（FR-70，fix + 增强 FR-6/FR-53/FR-51，纯前端）：环境（namespace）的显示名 `name` 此前在各处筛选下拉未被利用、只显示编码。现各页（看板 / 实例 / 代理 / 拓扑 / 审计 / 区分配 / 配置中心及其对话框）的环境下拉统一显示「编码 · 名称」。实现上增强 `Combobox` 支持「值 / 显示分离」的候选（`{value,label}`，**向后兼容既有 `string[]` 用法**），**筛选与提交的实际值仍是纯编码**（不采用拼串后再切分的脆弱方案），故 API 过滤值与空值聚合语义不受影响。
- 区分配改派安全化（FR-71，增强 FR-8/FR-35，[ADR-0036](docs/adr/0036-zone-reassign-safety-drain-gate.md) 扩展 [ADR-0004](docs/adr/0004-zone-authority-control-plane.md)，见 [docs/specs/zone-reassign-safety.md](docs/specs/zone-reassign-safety.md)）：归派看板此前「拖拽即改」无确认，把**在场有玩家**的服改区会热更其有效配置覆盖链、扰动玩家，且拖拽易手滑误触。现三管齐下——① **后端排空门硬闸**：目标服在线（`online`）且在场有玩家（`playerCount > 0`）时，凡会改变其区归属解析的操作（改派到不同区 / 取消指派 / 首次指派）一律 `409 ZONE_SERVER_ONLINE_NONEMPTY`（须先 drain 或等玩家离开排空再操作）；**同值指派 no-op**（不落库 / 不审计 / 不唤醒）；真正发生的变更仍入审计；**后端是唯一安全权威**（前端可绕、此硬拒绕不过）。② **前端高摩擦**：移除 @dnd-kit 拖拽归派 → 归派看板**默认只读**、须「解锁改派」开关显式开启，改派走对话框且要**手输目标 serverId 原样复述**方可提交（空服 / 离线服也要确认，像删库手打仓库名），取消指派亦须二次确认；被 409 拒时提示先排空。③ 面向用户「zone 分配」更名「**区分配**」、「zone」改「区」（纯 i18n 展示）。**REST 契约（路径 / 字段）、`zone_assignment` 表与唤醒机制、审计 action 枚举值、agent 行为一律不变**——只加安全前置与展示更名。
- 反向抓取升级为受管任务 + 两段式 scan/submit（FR-58，增强 FR-39，[ADR-0037](docs/adr/0037-reverse-fetch-managed-task.md) 取代 [ADR-0027](docs/adr/0027-reverse-fetch-channel-and-security.md) 决策5 / 扩展决策1，见 [docs/specs/reverse-fetch-managed-task.md](docs/specs/reverse-fetch-managed-task.md)）：反向抓取此前是一次性命令、**超限整批失败**——`plugins/` 下混入的运行时数据文件（>1MB 的 `.jsonl` 指标 / ServerProbe 落盘）单个就把整次抓取拖垮（真机 100% 失败）。现重构为**受管任务**（新 `reverse_fetch_task` 实体 + 状态机 `scanning→pending-review→fetching→ingesting→done/failed/cancelled/expired` + 进度，**单实例同时仅一个活跃任务**、再触发 `409 REVERSE_FETCH_TASK_ACTIVE`）+ **两段式命令**：① scan 阶段 agent 遍历 `plugins/` **只采元信息**（path / size / 是否文本 / 是否超阈值）**不读内容、永不因超限失败**，超限运行时文件在清单里**红标**而非拖垮扫描；② 人工选定后 submit 阶段 agent **仅回传选定 path 的内容** ingest 落库（复用 `FileService.Import`），超阈值文件**须显式确认才纳入**（只拒该文件、不拒整批）。改 agent-core（命令载荷加 `mode`/`selectedPaths`、scan 只 `stat` 不读内容、submit 按子集回传，**双端 jar 重建**），全程 async 不碰 MC 主线程、纯读不写、ADR-0027 安全边界（限 `plugins/`、禁逃逸、排除 `.jar`/二进制、双校验、detail 不含内容）一条不松。新增 admin 端点（建扫描任务 / 查任务 / 提交选定 / 取消）+ agent `POST /beacon/v1/agent/files/scan` 清单回传端点。FR-59（忽略规则 / 冲突 diff）、FR-60（审核台 / 任务台前端）在此底座上叠加。
- 反向抓取审核：持久忽略规则 + 冲突 diff 确认（FR-59，增强 FR-39 依赖 FR-58，沿用 [ADR-0037](docs/adr/0037-reverse-fetch-managed-task.md) + 复用 FR-46 范式，见 [docs/specs/reverse-fetch-review.md](docs/specs/reverse-fetch-review.md)）：在 FR-58 受管任务两段式底座上加审核——① **持久忽略规则**（新 `reverse_fetch_ignore_rule` 表，按 ns / 大区 / 实例，`exact` 单文件 / `prefix` 目录前缀；扫描清单返回时对命中当前任务作用域活跃规则的文件标 `ignoredByRule`，让运行时垃圾下次扫描自动可见可排除）；② 入库前**目标已有版本则进 `conflict-review` 状态**（暂存本次抓取内容、不立即落库），出逐文件 **diff**（抓取值 ⟷ 已有版本），用户逐冲突决定**保留哪份**——取抓取须 `reviewedMd5` 自审门（复用 FR-46 拓印自审、盲确认 `412`）、保留已有则跳过；resolve 后非冲突集 + 确认覆盖集**原子落库**（复用 `FileService.Import`）。忽略规则增删 / 冲突确认 / 提交入审计。**纯后端**（agent 不改）。新增端点：`ignore-rules` CRUD、`tasks/{id}/conflicts`、`conflicts/diff`、`resolve`。
- 反向抓取审核台 + 任务台前端（FR-60，增强 FR-46 依赖 FR-58/59，沿用 [ADR-0037](docs/adr/0037-reverse-fetch-managed-task.md) + 复用 FR-46 拓印审核台范式，见 [docs/specs/reverse-fetch-console.md](docs/specs/reverse-fetch-console.md)）：新增管理台「反向抓取」页（`/reverse-fetch`），把 FR-58/59 的受管任务工作流变可用——**任务台**（建扫描任务 选在线源 + 作用域[组/实例] + 目标；任务历史 + 状态机/进度 Badge + 取消）+ **审核台**（任务 `pending-review` 时全量列扫描清单逐文件 path/大小/是否文本/**超阈值红标**/`ignoredByRule`；超阈值文件须勾确认才纳入；逐项 / 目录前缀**忽略**[临时勾除 + 「保存为持久规则」调 `createIgnoreRule`]；选定后提交）+ **冲突审核**（任务 `conflict-review` 时列冲突 path、逐文件 Monaco DiffEditor 看「抓取值 ⟷ 已有版本」、勾「我已审阅」过自审门后逐文件「取新值(overwrite)/保留已有(keep)」、resolve 落库）+ **忽略规则管理**（列 / 建 / 删，`exact` 单文件 / `prefix` 目录前缀）。最大化复用 FR-46 拓印审核台的轮询 + Monaco DiffEditor + 自审门范式。**纯前端**，消费 FR-58/59 端点、未改后端 / 契约。
- 可观测看板瘦身为「集群粗览」（FR-64，增强 FR-32/FR-43，纯前端，见 [docs/specs/monitoring-restructure.md](docs/specs/monitoring-restructure.md)）：看板**删两张逐实例明细表**，收紧为一屏「集群粗览」——保留 总人数 / 在线服数 / 集群均值（TPS·内存·CPU）与 BC 维度（代理数 / 连接 / 线程 / 可达率 / 延迟）两组 KPI + 关键趋势（4 折线）+ **在线分角色摘要**（子服 N / BC 代理 M）+ **健康分布**（online/lost/offline 由 `listInstances` 按 status 前端计数）+「服务器详情 → /servers · 拓扑 → /topology」入口；逐服深数据移交 FR-65。环境筛选 + 一键清空（FR-63）保留。
- 服务器页：实例与健康 + 代理服管理合并（FR-65，增强 FR-52 + FR-5/FR-33/FR-49，纯前端，见 [docs/specs/monitoring-restructure.md](docs/specs/monitoring-restructure.md)）：「实例与健康」+「代理服管理」两页合并为统一「**服务器**」页（`/servers`，旧 `/instances`、`/proxies` 重定向至此）。统一列**全部 serverId（bukkit + bungee）**：角色 / 大区 / 小区（未分配黄高亮）/ 状态（健康 Badge）/ 地址 / 版本 + 角色相关深指标（bukkit 人数·TPS；bungee 连接·运行时长·后端可达）+ drain 态 + 最近心跳；筛选 ns / 组 / 区 / 角色 / 状态。单服详情 Sheet 按角色分区展双类深指标（bukkit 容量 / 权重 / appliedMd5 / 默认入口…；bungee 连接 / 线程 / 运行时长 / 后端可达·延迟 + 后端清单 + 所属小区默认入口）。操作：下线 / 取消下线（FR-49）、**drain / undrain**（前端首次接入 `scheduling/drains` 端点）、**区改派**（复用 FR-71 `ReassignDialog` 的排空门 + 手输确认，仅 bukkit 出该操作）。删 `InstancesPage` / `ProxiesPage`。复用 FR-52 已齐的逐服深指标、未改后端 / 契约。
- 配置变更审批化（FR-66 增强 FR-38 + FR-67 增强 FR-1/FR-3/FR-18，纯前端，见 [docs/specs/config-change-approval.md](docs/specs/config-change-approval.md)）：给配置上传与在页编辑加「提交前看一眼」的防误步。① **上传预览审批**（FR-66）：上传文件夹 / 多文件选完**不直接传**，先弹预览模态全量列待传文件（path / 大小 / 二进制·超大 Badge + 前端 `FileReader` 读文本内容预览·截断防卡），「我已审阅」确认才调 `importFiles` 入库、取消不入库（防误传）。② **在页编辑确认**（FR-67）：单文件在页改配置点保存**不直接发布**，先弹确认对话框展示配置三元组 + Monaco diff（编辑态 ⟷ 上一已保存版本）+ 备注，确认才 `publishConfig`、取消不发布（防误改）；批量 / 上传走 FR-66 预览审批。复用反向抓取审核清单 / 拓印自审闸 / CodeEditor diff 范式，**纯前端**、未改后端 / 契约、不引多人审批实体。
- 文件树预览全量 + 追踪态（FR-68，增强 FR-45 与 FR-58 共用扫描，纯前端，见 [docs/specs/file-tree-full-preview.md](docs/specs/file-tree-full-preview.md)）：文件树预览页（FR-45）此前只列「Beacon 管理的有效文件树」，看不见磁盘上有、Beacon 没管的文件。新增「全量预览（含未追踪）」态——对所选在线源复用 FR-58 受管扫描（`createScanTask` scope=server → 2s 轮询 `getReverseFetchTask` 至 `pending-review` → 读全量磁盘清单 path/大小 → 读完即 `cancelReverseFetchTask`，预览只读不 ingest），与有效树（`effectiveFiles`）交叉比对：清单文件 ∈ 有效树标 **追踪**（可点开复用 FR-45 合并卡片看合并结果 / 逐键来源）、∉ 有效树标 **未追踪**（磁盘有 Beacon 没管，仅列 path/大小 + 「去反向抓取纳管」入口跳 `/reverse-fetch`）；追踪 / 未追踪 Badge 清晰可分。从 FR-45 抽 `FileMergeCard` 供两态共用消重。**纯前端**，复用 FR-58/60 扫描客户端 + FR-45 有效树、未改后端 / 契约。
- 服务分析 / 平台用量看板（FR-73，依赖 FR-72，见 [docs/specs/service-analysis.md](docs/specs/service-analysis.md)）：新「服务分析」页（`/service-analysis`），把 FR-72 全量审计落库的 `audit_log` 聚合成**平台运维活动**视角——区别于 FR-32（MC 负载遥测）/FR-30（Prometheus），它看「人 / 系统对平台做了什么」。后端新增聚合端点 `GET /admin/v1/audits/analytics?namespace=&from=&to=`：按时间窗（缺省近 30 天、**上限 92 天**超出 400）+ 环境过滤，返窗口内总操作数 / 成功数 / 失败数 + 按动作分布（降序）+ 每日趋势（UTC 日）。**可移植 GORM**——repo 仅投影 `created_at/result/action` 行、**日分桶 / 计数 / 成功率全在 Go 侧 service 算**（禁 `DATE()`/`date_trunc` 方言函数，保 Postgres 可切），分层 router→handler→service→repository、handler 不碰 GORM。前端复用 FR-32 看板范式（环境 Combobox 可清回全部 + 时间窗 Tabs 近 7/30 天 + StatCard KPI 网格 + recharts 趋势折线 / 动作排行柱状），动作枚举复用 FR-7 审计页既有中文 i18n 映射。集成测试真连 MySQL 验聚合 / 过滤 / 窗口上限 / 空结果序列化。

### 修复
- 审计动作中文标签补全反向抓取受管子动作（修复 FR-7/FR-58）：审计日志页与服务分析页（FR-73）的「动作」列把 `file.reverse-fetch-scan/submit/ingest/cancel`（FR-58 受管任务两段式新增的写动作枚举）直接显原始英文 key——这几个枚举随 FR-58 落库但当时漏配 `audit.action.*` 中文映射（真机服务分析页动作分布暴露）。补 4 条映射（反向抓取·扫描 / 提交 / 落库 / 取消），审计 / 分析两页动作列恢复全中文。纯前端 i18n。
- 文件树有效预览页选服后整页白屏（修复 FR-45）：整文件来源（wholeFile，无键路径）的逐键来源 `path` 经 `toProvViews` 直传 nil 切片，序列化为 JSON `null`；而前端契约声明 `path` 为 `string[]`，`FileEffectivePreview` 的 `src.path.length` 对 `null` 抛 `TypeError` 致 React 整页白屏（真机选服即触发；前端单测此前用 `path:[]` 模拟数据，未覆盖后端真实下发的 `null`，故验证门漏掉）。控制面 `toProvViews` 把 nil path 归一为 `[]`（配置有效预览 FR-22 与文件树预览 FR-45 共用此转换，`sources` 与 `deletions` 的 path 一并受益）；补 `toProvViews` 序列化单测守护 `path` 恒为 `[]` 非 `null`。
- 可观测看板环境筛选可一键清回全部聚合（修复 FR-63，纯前端）：看板环境筛选选中某环境后无法清回「全部聚合」（空值 = 聚合全部）。为 `Combobox` 增加可选清空能力（值非空时显示清空按钮、点击置空），看板启用之；清空即把环境置空、指标按全部环境聚合刷新。

## 0.9.1（2026-06-23）

### 修复
- agent 文件树原子落盘在 Windows 上并发竞争致间歇异常（增强 FR-14 / FR-57，agent 侧，控制面不涉）：真机 E2E（Paper 1.20.1 + BeaconAgent 0.9.0）暴露——文件树落盘的「写临时文件 + 改名」原子写在 Windows 上间歇抛 `NoSuchFileException`（如 `file-tree.applied.json.tmp -> file-tree.applied.json`）异常栈、首次落盘可能失败（下个同步周期才自愈）。根因：`AppliedFileManifestStore` / `FileMirrorWriter` / `SnapshotStore` 三处各自用**固定临时文件名**（`*.tmp` / `*.beacon-tmp`）做原子写，而 `FileTreeApplier.apply` 可由文件树长轮询循环 / SSE `file-changed` / 运维 `resync` 三路并发触发——两轮落盘抢同一临时文件，一方 `Files.move` 走后另一方 `move` 找不到源即抛；且原子写未对 `ATOMIC_MOVE` 不支持 / 目标被杀软·索引器短暂占用做兜底，异常还直接逃逸到 Bukkit 调度器。修复：抽出统一原子写原语 `AtomicFileWriter`（**唯一 UUID 临时文件名**杜绝并发抢占、`ATOMIC_MOVE` 不支持时回退非原子 `REPLACE_EXISTING`、瞬时 `FileSystemException` 与多线程并发覆盖同一目标的 `AccessDeniedException`（Windows `MoveFileEx` 共享冲突）走**抖动退避**有限重试（抖动打散并发线程的重试对齐、避免反复撞同一目标锁窗口）、失败清理临时文件不残留、父目录 best-effort fsync），三处落盘器统一改走该原语（顺带补齐 [ADR-0010](docs/adr/0010-file-tree-hosting-blob-channel.md) 决策4 所记 `SnapshotStore` 落盘未 fsync 的缺口）；`FileTreeApplier.apply` 加锁**串行化**消除清单读改写竞态，末尾清单写入失败按 fail-static 兜住（保留既有镜像、下轮重试，不抛未捕获异常到调度器）。新增并发落盘单测（`FileMirrorWriterTest` / `FileTreeApplierConcurrencyTest`，旧实现下稳定复现、修复后 5 次重跑全绿）把此前仅真机暴露的竞态下沉为自动化回归。
- 看板「子服(bukkit)」区块「在线服务器数」错含 BC 代理（修复 FR-43）：`GET /admin/v1/metrics/summary` 的 `onlineServers` 此前取全部在线实例数，致子服区块显示的「在线服务器数」把 bungee 代理一并计入（如 1 子服 + 1 代理显示 2）。改为仅计 `role=bukkit` 子服（与该区块语义及平均 TPS·内存·CPU 口径一致；代理数另由 `bc.proxyCount` 表达），`API.md` 同步口径说明。
- 控制面看板 / BC 面板 / 趋势在配置稳态下负载指标全为 0（修复 FR-32 / FR-34，agent 侧）：agent 此前**仅在配置长轮询 / SSE 返回 200（配置 md5 变更）时**才上报一帧负载指标，稳态配置不变恒 304 → 采集供给（人数 / TPS / 内存 / CPU 与 BC 专属 proxy 指标）从不被调用 → 控制面内存注册表里每实例的负载指标恒为零值，采样器忠实地把这堆零值每 tick 落 `metric_sample`，致看板总览 / BC 面板 / 历史趋势全 0（仅在频繁改配置时偶现真值，且控制面重启后注册表清零即再不回填）。真机实测此缺陷下运行中实例的 `uptimeMs` / `threadCount` / `tps` 等恒为 0（JVM 运行时长不可能为 0，正是采集从未执行的铁证）。修复：`AgentLifecycle` 新增独立的**周期性指标上报循环**（与心跳同周期、但与配置变更解耦），注册成功后首跳即发（重连 / 重启后尽快点亮看板真值）、其后每心跳周期续杯，把当前一帧负载指标持续刷进控制面注册表；全程经 `runAsyncDelayed` 异步、绝不阻塞 MC 主线程，采集 / 上报失败均已回退（fail-static 不变）。新增「配置稳态 304 下周期循环仍持续上报真实指标」单测复现并守护（修复前该用例 `reportCalls` 恒 0）。

### 变更
- BC 后端可达性探测由 MC status-ping 改 TCP 连接（FR-34，[ADR-0035](docs/adr/0035-backend-reachability-tcp-connect.md) 修订 ADR-0025 决策1/3 的探测机制，见 [docs/specs/bc-proxy-metrics.md](docs/specs/bc-proxy-metrics.md)）：真机暴露——在线、玩家可正常连入的代理后端常因开启转发（bungeecord / 现代 proxy forwarding）而**不应答** MC server-list-ping（外部裸 status-ping 实测 TCP 握手成功但 status 请求 recv 超时），致 `ServerInfo.ping` 对可达后端**恒判不可达**（看板「后端可达性」恒 0/N，加大 ping 超时无效——后端根本不应答而非慢）。改为对代理目录里每个后端的 socket 地址并发发起一次带超时的 **TCP 连接探测**——端口可连即可达（RTT 取连接耗时）、连接被拒 / 超时即不可达；下沉为 core 平台无关纯 JDK `TcpBackendProbe`（与聚合纯函数 `BackendReachability` 同包、含真实 socket 单测），bungee 壳 `BungeeProxyMetricsCollector` 仅取各后端 `socketAddress` 委托、在守护线程池并发**不碰 MC 主线程**。可达性 / 延迟语义、上报契约、`metric_sample` 落库列、前端展示**零改动**（仅换探测实现）。另加 `[BC可达性]` 去重 WARN：探测异常 / 空目录时变化才打一条便于运维定位（不刷屏）。
- 管理台各页筛选统一为可编辑下拉框 + 进入即默认拉全部（增强 FR-51，纯前端）：实例与健康 / 代理服 / zone / 看板等页的环境 / 大区 / 小区筛选此前为裸文本框且需手动输入才出数据。现统一为带下拉箭头指示、候选来自系统（namespaces / instances / zone 汇总）的可编辑 `Combobox`，进入页面即以「全部」（留空聚合全部环境）默认拉取数据、不再要求先输入；候选复用既有 list 端点，无后端改动。

## 0.9.0（2026-06-23）

### 变更
- 文件树结构化深合并改无损（FR-57，增强 FR-44，[ADR-0034](docs/adr/0034-file-tree-lossless-merge.md) 取代 ADR-0029「值归一化可接受」一条，见 [docs/specs/file-tree-lossless-merge.md](docs/specs/file-tree-lossless-merge.md)）：通道B 多层结构化文件（`.yml`/`.yaml`/`.json`/`.properties`）的按键深合并此前复用配置中心 `merge.MergeDataID`，会 parse→reserialize **归一化叶子标量值并丢注释**（`007`→`7`、`1.10`→`1.1`、`2026-06-22`→时间戳、JSON 大整数失精度 `…678`→`…680`）——而通道B 托管的是第三方插件**死认文本**的磁盘配置，归一化是 bug。现改为**无损深合并**：`internal/merge` 新增 `MergeDataIDLossless`/`MergeDataIDLosslessWithProvenance`（**YAML 走 `yaml.v3` 的 `yaml.Node` 节点级递归合并、保叶子原文 token 与 Tag/Style 及头/行/脚注释；JSON 走 `json.Decoder`+`UseNumber()` 保大整数/浮点精度；properties 行式保 key 前置注释与原值文本**，不引新依赖），`internal/filetree` 的深合并分支（`Resolve` 与 `ResolveWithProvenance` 两处）改调无损版。**合并语义完全不变**（标量覆盖 / map 深合并 / list 整替 / 高层 `null` 删键 / 确定性键序与 md5 幂等，由「无损渲染再 parse 与 `MergeDataID` 类型模型逻辑相等」交叉测试钉死），**单层短路字节透传 / `wholeFileOverride` 豁免 / 坏内容回退整文件**三条不变（另：含锚点 / 别名 / `<<` 合并键的 YAML 走整文件回退最高层、不深合并，避免节点级合并产出悬空别名坏文件；properties 无删键、值 `null` 当普通字符串保留，均与有损语义对齐），`ResolveWithProvenance` 每文件 `content`/`md5` 仍与 `Resolve` 逐一致（FR-45 交叉测试守护）。**配置中心（通道A）维持 `MergeDataID` 有损不动**（类型化存储归一化可接受，且改它会让全集群配置首轮 md5 变→全量重拉，代价不可接受）。⚠️**升级 churn（一次性）**：多层结构化文件的合并后 md5 由有损渲染改为**无损渲染**（比 FR-44 又变一次），控制面升级后 agent 首轮一次性重取重写盘（内容更正确）；**单层文件 md5 不变**（仍字节透传）。

## 0.8.0（2026-06-22）

### 新增
- 文件树结构化深合并 + 按文件整文件豁免（FR-44，[ADR-0029](docs/adr/0029-file-tree-structured-deep-merge.md) 取代 ADR-0010 决策1，见 [docs/specs/file-tree-deep-merge.md](docs/specs/file-tree-deep-merge.md)）：通道B 文件树此前对每个 path 整文件覆盖，无法表达「一份基线 + 各层小增量」。现结构化文件（`.yml`/`.yaml`/`.json`/`.properties`）**当有 ≥2 层贡献时**跨 global/大区/小区/单服 四层**按键深合并**（复用 `internal/merge`：标量覆盖 / map 深合并 / list 整替 / 高层 `null` 删键 / 固定键序与 md5，与配置中心同一套语义与幂等），把通道A 的深合并能力带给「无法接入 agent API」的第三方插件——控制面渲染合并后整文件、agent 镜像落盘逻辑零改、第三方插件照旧从磁盘读到合并结果并热重载。**单层结构化文件字节原样透传**（不 parse/reserialize，杜绝 `007`→`7`/日期转时间戳/`1.10`→`1.1`/JSON 大整数精度丢失/纯注释清空等有损往返）。非结构化文件（脚本 / lang 等）维持整文件覆盖；`file_object` 新增 `whole_file_override` 标记（API `POST /admin/v1/files` 可传 `wholeFileOverride`、文件视图回显），**任一层标即整 path 强制整文件覆盖**（保注释、不被重渲染打乱）。结构化文件坏语法在 Create/Publish/Import 即被拒 `CONTENT_SCHEMA_INVALID`（与通道A 发布校验对齐）；运行期某层仍解析失败则该 path 回退整文件取最高层（兜底、一坏不拖垮整树）。manifest 的单文件 md5 改为**合并后整文件**的指纹。⚠️**向后不兼容**：**多层**结构化文件按键合并会经 parse→reserialize **归一化值**（前导零 / 日期 / 版本号 / JSON 大整数等，详见 ADR-0029 后果），升级后生效结果可能变、注释丢失；单层文件不受影响。现网升级前须复核**多层**结构化托管文件、对不宜合并者标 `wholeFileOverride`；升级首轮多层结构化文件会因 md5 语义变化一次性重取重写盘。
- 文件树有效预览 + 逐文件/逐键来源 provenance（FR-45，沿用 [ADR-0013](docs/adr/0013-admin-effective-config-preview-and-provenance.md) 模式扩展到 [ADR-0029](docs/adr/0029-file-tree-structured-deep-merge.md) 文件树合并，见 [docs/specs/file-tree-effective-preview.md](docs/specs/file-tree-effective-preview.md)）：把 FR-22 的有效配置预览 + 逐键来源扩展到通道B 文件树。新增 admin 只读端点 `GET /admin/v1/files/effective?namespace=&serverId=&group=&zone=`（形参对齐 `configs/effective`），预览某目标合并后的有效文件树——逐文件给出合并 `content`/`md5` + 来源：结构化非豁免文件按 `merge.MergeDataIDWithProvenance` 产出逐叶子键最终来源层（global/大区/小区/单服）与被高层 `null` 减量删除且最终不存在的键；非结构化 / 标 `wholeFileOverride` 豁免 / 坏内容回退的文件标 `wholeFile=true`、来源为单条空路径（= winner 层）。provenance 经 `filetree.ResolveWithProvenance` 平行纯函数计算（与 `Resolve` 同一套 per-path 分桶 + winner + 分流判定），**不改 `Resolve`/`merge` 这条 agent 下发热路径**；以「每个 path 的 `content`/`md5` 对任意候选集恒等于 `Resolve`」交叉测试钉死、防双实现漂移。端点不挂长轮询、不强制注册（同 FR-22 克制）。管理台新增只读「文件树预览」页（`/file-preview`，选服 → 列文件 → 看合并结果 + 逐键/整文件来源徽标 + 豁免与被删键标注），作为后续 FR-46 审核台 diff「期望合并值」一侧的数据源。
- git 单向导出镜像（FR-47，[ADR-0030](docs/adr/0030-git-export-mirror.md)，见 [docs/specs/git-export-mirror.md](docs/specs/git-export-mirror.md)）：在不动真源、不引重型件的前提下，把配置 / 文件树的**源层**（global/大区/小区/单服 各层原始内容，非合并后的有效配置）按目录结构**单向导出**到一个 git 仓——备份 / 灾备 / 外部可见、可 `git log` 翻历史。**git 仓是单向派生镜像、绝不作第二真源**：数据只从 MySQL 单向流向 git，永不回灌、不参与 agent 下发或有效配置解析（守架构不变量 #3）。发布 / 回滚 / 改派事务**提交成功后**、与 `ChangeNotifier` 长轮询唤醒**并列**触发导出（可选注入 `SetGitExporter`，未注入即 no-op），导出在**单 worker goroutine 串行异步 best-effort** 跑——发布请求线程只投信号立即返回、绝不做 git IO，git 任一步失败**仅 WARN、绝不回滚发布 / 绝不阻断主流程 / 绝不影响下发**。commit message 带审计信息（操作者 / 动作 / 对象 / 版本）。**敏感不泄密**：FR-20 标 `sensitive` 的配置项导 `enc:v1:` **密文**（导出源直读原始行、不解密，密钥不入 git）；`file_object` 新增 **`sensitive_excluded` path 级标记**（`POST /admin/v1/files` 可传 `sensitiveExcluded`），置真则该文件**整体不导出**到 git（库内保留、下发不变，防反向抓取来的第三方插件明文 DB 密码落 git）；远程推送凭据走 env（`BEACON_GIT_EXPORT_REMOTE_TOKEN`）不入库。新增 `internal/gitexport` 纯逻辑包（源层→快照 / 目录布局 / 敏感排除 / commit message，与 `merge`/`filetree` 平级、可穷举单测）+ `GitRepo` 端口接口隔离真正读写 git（与 ADR-0005 同构）。git 写入用 **go-git**（`github.com/go-git/go-git/v5`，纯 Go、契合单二进制 alpine、无需镜像装 git）经 `GoGitRepo` 适配实现。新增配置块 `git-export`（默认关）。灾备恢复走手动 / 脚本化重导入（保持单向、不引入入站同步 / 自动 restore）。
- 小区默认入口 + BC 默认服注入（FR-48，[ADR-0031](docs/adr/0031-zone-default-entry-and-bc-injection.md)，见 [docs/specs/zone-default-entry-bc-injection.md](docs/specs/zone-default-entry-bc-injection.md)）：修复「BC 动态注入子服却从不设默认/fallback 服 → 玩家加入报 `Could not connect to a default or fallback server`」。每个小区 `(group, zone)` 可在控制面唯一指定一个「默认入口」serverId（DB 权威、应用层校验须为已指派该 zone 的子服）。新增 `zone_default_entry` 表（GORM 可移植、`VARCHAR`+时间戳、`(namespace,group,zone)` 唯一、零方言）；新增 admin 端点 `GET/PUT/DELETE /admin/v1/zones/default-entry`（写端点 readonly→403），set/clear 在事务内连同审计（`zone.set-default-entry` / `zone.clear-default-entry`）原子完成、提交后唤醒拓扑 watch。发现（`/beacon/v1/agent/discovery`）与实例视图新增布尔字段 `zoneDefaultEntry`（命中默认入口的 bukkit 为 true、其余 false、bungee 恒 false；渲染时由 `ZoneService` 读 DB 标记、`InstanceService` 经注入解析器获取，handler 不碰仓库；旧 agent 忽略未知字段向后兼容）。BC agent 既有「同步子服目录」async 循环注入子服后，按纯逻辑 `DefaultEntrySelector` **只**据本代理 `config.yml` 的 `proxy.home-group`/`proxy.home-zone`（数据面路由配置，声明「本代理服务哪个小区」，非 zone 归属声明，不违反 [ADR-0004](docs/adr/0004-zone-authority-control-plane.md)）命中的在线默认入口选默认服——再把它置于每个 BungeeCord 监听器 `server-priority` 列表首位（公开 API、幂等去重、不反射）。**未配 home-zone / 该 zone 在 Beacon 未设默认入口 / 默认入口当前不在线 → BC 不设任何默认服，并打一条带 group/zone 上下文的中文 WARN**（去重不刷屏，告知运维去 Beacon 配默认入口）；玩家此时遇 BungeeCord 原生「无默认服」拒绝（明确信号），**绝不被静默落到非大厅服**（不再回退「取首个在线 bukkit」——静默落错服跳过大厅有风险）。多台 BC 配同一 home-zone 共享同一默认入口。注入子服 + 设默认服打 INFO 中文日志。agent-api `ServiceInstance` 加 `zoneDefaultEntry`（缺键 false，向后兼容）。
- 实例主动下线态（FR-49，见 [docs/adr/0032-instance-active-offline-state.md](docs/adr/0032-instance-active-offline-state.md) 与 [docs/specs/instance-active-offline-state.md](docs/specs/instance-active-offline-state.md)）：管理台「下线」此前只删内存条目，agent 下一跳即重连复活、日志刷屏。现做成**完整下线态**——新增 `server_offline` 拒绝表（与 `server_drain` 同构、软删可移植），主动下线事务内落 DB 拒绝态 + `instance.offline` 审计、提交后移出内存可用集；注册（register）前查拒绝表，命中返**专门错误码 `403 INSTANCE_OFFLINE_REJECTED`**（区别于 `404 NOT_REGISTERED` / `409 DUPLICATE_SERVER_ID`，重复 serverId 守卫不受影响），控制面重启仍生效。agent 收 `403` 进新生命周期态 `OFFLINE`：停止退避猛打、改按大间隔降频探测重注册、仅首次进入 WARN 一次不刷屏，**与 `DEGRADED`（控制面不可用）严格区分**、fail-static 不变（不清快照、不阻断玩家）。新增 `DELETE /admin/v1/instances/{serverId}/offline`（取消下线，软删 + `instance.online` 审计，无标记→`404 OFFLINE_NOT_FOUND`）与 `GET /admin/v1/instances/offline`（列下线标记）；下线/取消下线均为写方法（readonly→403）。**心跳热路径不查库**（下线在线实例移出内存→心跳 404→重注册被拒收敛，无每跳 DB IO）。**主动下线 ≠ drain（排空、仍可连）≠ 健康 TTL（自动衰退）**，三者职责分离。管理台「实例与健康」页下线改为**按行直接操作**（namespace 取自该行、不再强制先筛环境），并新增「已主动下线（拒绝接入）」区块支持取消下线。⚠️真机维度（真 agent 被拒后停止重连、取消后恢复）待真机验证。
- 按需拓印回写 + 前端审核台（FR-46，沿用 [ADR-0027](docs/adr/0027-reverse-fetch-channel-and-security.md) 命令通道 + [ADR-0013](docs/adr/0013-admin-effective-config-preview-and-provenance.md)/[ADR-0029](docs/adr/0029-file-tree-structured-deep-merge.md) 合并与来源，未引入新 ADR，见 [docs/specs/imprint-review-console.md](docs/specs/imprint-review-console.md)）：把 FR-39「在线实例反向抓取」从「抓上来即落库」升级为**可控审核台**。复用 FR-39 命令通道（`agent_command` + SSE `command-pending` + agent 读整棵 `plugins/` 树回传，**agent 零改动**），命令载荷加 `mode=imprint` + 目标 `path`：admin 选一台在线服 + 一个文件触发拓印 → 控制面收到回传时**不 ingest 落库**，而是从回传树取该 `path` 的磁盘原文**转存到命令瞬态列 `imprint_content`**、命令转新状态 `ready`（指定 path 不在回传树中即 `failed`）。admin 拉 `GET /admin/v1/imprints/{commandId}/diff?scope=&group=&zone=&target=` 看**本地实际内容 ⟷ 期望合并值**（期望侧复用 FR-45 `ResolveWithProvenance` 按并入层视角解出该 path 的覆盖链合并 `content`/`md5` + 逐键来源）；选并入层（单服/小区/大区/全局）+ 目标键。`POST /admin/v1/imprints/{commandId}/confirm` 带 `reviewedMd5` 过**单人自审门**——须等于命令转存内容 md5（看过 diff 才拿得到，盲确认 → 412 `IMPRINT_REVIEW_MISMATCH`），通过后复用 `FileService.Create`（该层 path 首次）/`Publish`（已存在）落为**该层整文件覆盖**、写 `file.imprint` 审计、命令转 `done` 并清空瞬态内容 → 走通道B 既有长轮询 / SSE 正常下发（控制面**零新增下发路径**）。命令状态可经 `GET /admin/v1/imprints/{commandId}` 轮询至 `ready`。触发记 `file.imprint-fetch`、确认落库记 `file.imprint`（detail 均**不含文件内容**，沿 ADR-0027 决策7）；瞬态内容确认 / 失败 / 过期即清（比 FR-39 永久落整棵树**暴露更少**）。**严守边界**：不做全自动 / 后台双向同步（改动必经控制面人确认，守架构不变量 #1）、不引入多人审批 / 变更请求实体（单人自审门即可）、不抓运行时数据文件（沿 FR-39 限 `plugins/` + 排除 `.jar`/二进制 + 上限 + 双校验）。管理台新增「拓印审核台」页（`/imprint`，选在线服 + 文件 → 触发 → 轮询至 ready → Monaco DiffEditor 展示期望 ⟷ 实际 + FR-45 逐键来源徽标 → 选并入层 + 预览 → 确认同步），复用 FR-23 既有 `CodeEditor` diff 模式（不扩重量级在线编辑）。
- 环境（namespace）增删改查补全（FR-53，增强 FR-6，未引入新 ADR，见 [docs/specs/namespace-crud.md](docs/specs/namespace-crud.md)）：环境此前只有列表 / 新建，补齐**改名**与**删除**。新增 `PUT /admin/v1/namespaces/{code}`（改显示名 `name`，`code` 为不可变身份键不改；环境不存在 `404 NAMESPACE_NOT_FOUND`）与 `DELETE /admin/v1/namespaces/{code}`（**删除守卫**：环境下有已注册实例→`409 NAMESPACE_HAS_INSTANCES`、有已指派 zone→`409 NAMESPACE_HAS_ASSIGNMENTS`、有配置→`409 NAMESPACE_HAS_CONFIGS`、有文件树→`409 NAMESPACE_HAS_FILES`、有覆盖集→`409 NAMESPACE_HAS_OVERRIDE_SETS`，各拒因清晰区分、命中即禁删且不审计；守卫全过才**硬删**——namespace 表无软删需求，删除仅在环境内已无在用数据时放行，无「同 code 软删后重建」诉求故不引软删列）；二者皆写方法，readonly 角色经 `readonlyWriteGuard` 403。改名 / 删除分别在事务内连同 `namespace.update` / `namespace.delete` 审计原子完成（operator 由认证态派生、detail 不含敏感数据）。守卫查在用数据复用既有能力：实例数读内存注册表（`Registry.CountByNamespace`）、zone 指派 / 配置项 / 文件树 / 覆盖集各读其仓库的 `CountByNamespace`（均按未软删计）。删除守卫含**文件树 / 覆盖集**——二者同属该环境的「配置数据」，且 `file_repo` 等纯按 `namespace_code` 字符串定位，若漏守卫硬删会留下孤儿行、运维同 code 重建环境后静默重新归属并下发，故纳入守卫以忠于 PRD「已有配置则禁删」之意图。管理台「环境管理」页在既有列表 / 新建上扩展每行「改名」（Dialog）与「删除」（二次确认 AlertDialog），守卫拒删的中文错误经消息提示。**边界**：不做级联删除、不改 `code`、不引 namespace 软删。
- 代理服管理页（FR-52，沿用 FR-34/FR-36/FR-48 既有决策，未引入新 ADR，见 [docs/specs/proxy-management-page.md](docs/specs/proxy-management-page.md)）：新建独立管理页 `/proxies`，集中、透明展示某环境下所有 BC（bungee 代理）运行态——状态 + 底层参数（在线连接数 / JVM 线程 / 运行时长 / 后端可达性·延迟，FR-34）、后端子服清单（FR-36）、所属小区默认入口（FR-48）、所属大区 / 小区，逐台一卡，让运维完整了解 BC 状态。复用既有端点：`GET /admin/v1/instances?role=bungee`（拉 BC 实例）+ `GET /admin/v1/zones/default-entry`（按 zone 索引默认入口）。配套把控制面**已采集、此前仅在 `metrics/summary.bc` 聚合暴露**的每台 BC `proxy` 指标补暴露到逐实例视图（`instanceView` 加 `proxy` 对象，仅 bc 非零、bukkit 恒零；加法且向后兼容、无新端点 / 新采集 / 新 DB 列）。纯只读呈现既有事实，不含玩家名单 / 身份，不做任何写操作 / 调度。所属小区默认入口按 **group+zone 复合键**匹配（默认入口唯一键为 `(namespace, group, zone)`、同名 zone 码在不同大区是不同小区），避免仅按 zone 索引时后写覆盖先写导致跨大区串值。⚠️浏览器目视维度待真机验证。

### 变更
- zone 指派表单改 API 拉取下拉并加校验（增强 FR-40）：`ZonesPage` 的「新增 zone / 指派」对话框此前环境 / serverId / 大区 / 小区全是手填文本框，易填错且无约束。现改为从系统拉取的下拉选择（环境来自 `listNamespaces`、大区 / 小区取 zone 汇总与实例列表并集、serverId 来自实例列表且仅列 bukkit 子服——与后端校验一致排除 BC 代理）；提交前校验必填项缺选与非法值（取值须落在 API 候选内），备注仍为自由文本。仅改输入方式，指派业务行为不变。
- 控制面状态页眉（FR-33）由横跨顶部的全宽页眉收进右侧主内容区顶部（侧边栏顶部保留「Beacon 管理台」品牌标题），纯 UI 重定位，指标内容与数据源不变。
- 可观测看板 BC / 子服双区块拆分（FR-43，扩展 [ADR-0023](docs/adr/0023-control-plane-observability-dashboard.md)，见 [docs/specs/dashboard-role-split.md](docs/specs/dashboard-role-split.md)）：完成 FR-32/FR-34 既定但未尽的角色分离。控制面把**平均内存**（`avgMemUsed` / `avgMemMax`）的口径由「÷全部在线实例」改为「仅 `role=bukkit`」，与平均 TPS·CPU 三者统一（消除 bc 堆字节混入子服内存均值的失真，无 bukkit 在线时内存均值为 0）；趋势 `Downsample` 桶内内存均值同步改为仅算 bukkit；`GET /admin/v1/metrics/summary` 的每服明细新增 `role` 字段（`bukkit` / `bungee`）。管理台 Dashboard 整体重组为「子服(bukkit)」与「BC 代理」两大区块——子服区为 bukkit 口径的总览卡片 + 趋势图 + 子服明细，BC 区为 bc 维度面板 + bc 明细，每服明细按角色分组呈现。仅聚合口径修正与展示重组，不新增指标 / 采集 / DB 列 / 端点，仍只展示负载数字、不含玩家名单 / 身份。
- agent `/beacon` 主命令帮助完善（FR-54，增强 FR-17，见 [docs/specs/beacon-command-help.md](docs/specs/beacon-command-help.md)）：双端壳新增 `help` 子命令（含权限提示的标题 + 用法首行 + 各子命令一行用法）、无参 `/beacon` 改打印完整用法（而非仅一行）、未知子命令 / 错参经 TabooLib 内置 `incorrectCommand` 回调回**中文**友好提示（回显出错片段 + 用法，取代默认中英双语 generic 文案），各子命令注册带 `description` 提升补全 / 帮助可发现性。全部帮助 / 提示文案仍单一收敛在 core 的 `OpsCommandText`（由 `Subcommand` 权威清单派生 `USAGE_LINES`/`HELP_LINES`，双端壳共用、防文案漂移，守 [ADR-0005](docs/adr/0005-agent-transport-codec-abstraction.md) core 不依赖平台库）；只补命令帮助 UX，不新增任何运维能力、不依赖远程下发（FR-11）。
- 维度输入统一可编辑下拉 combobox（FR-51，增强 FR-6/FR-37/FR-40，纯前端，见 [docs/specs/dimension-combobox.md](docs/specs/dimension-combobox.md)）：管理台「环境 / 大区 / 小区 / serverId」四类维度输入此前不一致——筛选框是裸文本框（无候选提示）、表单是原生 `<select>`（只能选不能输新值）。现统一为新增的 `Combobox` 组件（`web/src/components/ui/combobox.tsx`，基于既有 `radix-ui` Popover + Input 自实现、不引新依赖），按场景区分可编辑性：筛选框 / 可新建维度的表单为「可编辑」（候选来自 API 但允许键入列表外新值），纯选择处（zone 指派 / 反向抓取目标 / 配置 server 覆盖目标等须为已存在项）为「严格选」（键入仅过滤、不接受列表外值）。覆盖实例与健康页 / 审计页筛选、zone 指派表单、新建配置 / 导入到组 / 反向抓取对话框；集群拓扑页环境改为严格下拉并**默认选中第一个环境直接出图**（不再需手动输环境才出图）。候选来源复用既有 list 端点（namespaces / instances / zone 汇总），无后端改动、无新 ADR；role/status/format/覆盖层等枚举类下拉保持现状不动。
- zone 归派看板汇总区树形化（FR-55，增强 [FR-35](docs/PRD.md)，见 [docs/specs/zone-assignment-summary-tree.md](docs/specs/zone-assignment-summary-tree.md)）：`ZonesPage` 的 zone 汇总此前是页面**底部**一张把大区 / 小区拍平成同级行的扁平表格（大区 / 小区 / 服数 / 在线数）。现改为**页面上方**的「大区 → 小区 → 子服」三级**树形节点图**——大区为一级节点、小区为二级节点、子服为三级叶节点，集群归属层级一眼可读。服数 / 在线数仍**直取 `zoneSummary`（`ZoneStatView`）**、口径与原表格完全一致（服数 = DB 指派数、在线数 = 在线注册数），大区为其下小区合计；子服叶子复用既有看板模型 `buildKanbanModel` 的分桶结果（按 serverId 排序、BC 代理已排除），不另发请求。新增纯函数派生 `web/src/pages/zones/summaryTree.ts`（`buildSummaryTree`，计数取自 summary、子服来自模型并集，稳定排序，可穷举单测）+ 展示组件 `ZoneSummaryTree.tsx`（递归渲染、复用 `Badge` 与 ServerCard 同款在线状态点配色，零新依赖）。纯前端展现重组，**不动**拖拽指派 / 改派看板交互本体，后端零改动、无新 ADR。
- 前端 i18n 框架接入 + 全站文案 key 化（FR-50，增强 FR-6/FR-7/FR-21，[ADR-0033](docs/adr/0033-web-i18n-framework.md)，见 [docs/specs/web-i18n.md](docs/specs/web-i18n.md)）：管理台全部可见文案此前硬编码在各组件里，无单一真源、无法支撑多语言；审计 `action` 直接展示后端英文枚举（`config.publish` 等），运维不可读。现引入 **react-i18next + i18next** 完整框架（本期只交付 **zh-CN 全量**，框架就绪后加语言只需补一份资源文件、组件零改动），i18n **同步初始化 + 资源内联**（`lng`/`fallbackLng` 均 `zh-CN`，保证测试环境 `t()` 同步返回真实文案、不出裸 key）。全站硬编码中文（导航 / 标题 / 按钮 / 表头 / 占位符 / 空态 / 提示 / label / 对话框）搬到 `web/src/i18n/locales/zh-CN.ts` 经 `t('key')` 取用，key 分层（`nav.*`/`common.*`/各页域 + `status.*`/`role.*` label），同义文案归并 `common`（DRY）；zh-CN 值**逐字等于原文案**（等值迁移、渲染文本不变，既有断言中文的测试基本不动）。状态 / 角色 label 一并 key 化但**保留当前可见文本**（角色 `bungee→BC 代理`/`bukkit→子服` 中文不变；健康状态 `online`/`lost`/`offline` 等原本显示英文枚举的保留英文原值，不擅自改中文）。审计 action 经 `audit.action.<枚举>` 键族 + `defaultValue` 回退映射成中文（**后端不改、仍返英文枚举**），未知枚举回退原文不裸 key。zh-CN 资源成为前端文案单一真源。**纯前端、控制面零改动、未引入除 react-i18next/i18next 外的新依赖**。⚠️浏览器目视维度（各页文案正常、无裸 key）待真机验证。

### 修复
- 配置导入·上传 / 反向抓取含 agent 自身目录被整批拒绝（FR-38 / FR-39 归真，方案 D，见 [ADR-0028](docs/adr/0028-allow-hosting-agent-self-dir.md)）：真机 E2E 暴露——agent 反向抓取读盘必然带上自己的 `plugins/BeaconAgent/`（含 `config.yml` / 快照等），这些被 ingest 上传后命中控制面 `normalizePath` 的保留目录闸（commit `e7a0517` 引入），而 `FileService.Import` 是"任一不合法即整批拒绝"→ 整个 ingest `400 INVALID_PATH`、命令转 `failed`、零文件落库，**反向抓取对任何装了 agent 的在线服 100% 失效**；上传一份含自身目录的真实 `plugins/` 同样整批被拒。修复：**放开控制面 `normalizePath` 对 `BeaconAgent` / `BeaconAgentProxy` 顶段的拦截**（FR-41 env 注入已使 `config.yml` 非身份真源、托管自身目录无身份污染），自我保护下沉到 agent 侧唯一一道 observe-only 闸——`FileTreeApplier` 对自身目录顶段只观测不写回（不取 / 不写 / 不删，但写入 applied 清单视为已收敛避免 churn，commit `dcbbd94` 已在）；穿越 / 绝对 / 反斜杠 / 空仍硬拒（落盘逃逸边界不退化）。一处后端改动同修 FR-38 + FR-39，无需改 agent / 前端、无需重打 jar（详见 [docs/specs/file-tree-hosting.md](docs/specs/file-tree-hosting.md) §3.1）。
- zone 分配排除 BC 代理（FR-8/FR-35）：zone 是给 bukkit 子服的、BC 代理（role=bungee）不该有 zone，此前前后端零角色过滤、BC 可被误指派进 zone。现前端 zone 看板 `buildKanbanModel` 构建未指派池与 zone 桶时排除 `role==='bungee'`（BC 既不进未指派池、也不可拖入 zone 桶）；后端 `ZoneService.Assign` 做纵深防御——取注册表实例，确为 bungee 即拒为 `400 ZONE_NOT_ASSIGNABLE_TO_BC`（离线无角色事实可凭则沿用既有逻辑），bukkit 子服照常放行。
- 管理台主内容区滚动锁死：`Layout` 的 `<main>` 此前为 `overflow-hidden`，而它是定高 `h-screen` 列下的 `flex-1` 子项——普通堆叠页（看板 / 审计 / 实例 / 环境 / 拓扑 / zone）内容一旦超过视口高度即被整体裁在左上角、无法滚动看全。改为 `overflow-y-auto`：超高内容纵向可滚动；自管满屏页（配置中心 / 文件树预览 / 拓印审核台）以 `h-full` + 内部独立滚动适配、不会触发主区滚动条，行为不变。并修正配置落地页（`ConfigsPage` 根容器误用 `h-screen`=100vh，比 `<main>` 内容盒高约一截）改 `h-full` 消除主区多余滚动条与编辑器下方死滚动空间，与另两张满屏页一致。
- 可观测看板 KPI 卡片过大：子服 / BC 区顶部统计卡片（`StatCard`）此前 padding 偏大、主数值 `text-2xl`、图标 `size-5`、网格 `gap-4`，整体偏臃肿。现卡片改 `size=sm` 收紧内外边距、主数值降为 `text-xl`、图标降为 `size-4`、网格 `gap-3` 并收紧内部间距，视觉更紧凑协调；仅样式收紧，展示数据与口径不变。

### 安全
- 升级 `golang.org/x/crypto` 到 v0.52.0 + Go toolchain 到 1.26.4，清除 govulncheck 可达漏洞：FR-47 经 go-git 引入的 `golang.org/x/crypto/ssh` 系列（GO-2026-5017~5021 等，经 `GoGitRepo.push` 静态可达）+ go1.26.2 标准库项（`net/textproto`/`crypto/x509`/`net`/`net/http`，GO-2026-4918/4971/5037/5039）。`govulncheck ./...` 现报「0 vulnerabilities」、全量测试绿。**构建最低要求升至 go1.26.4。**

## 0.7.0（2026-06-22）

### 新增
- 配置导入·在线实例反向抓取（FR-39，见 [ADR-0027](docs/adr/0027-reverse-fetch-channel-and-security.md) 与 [docs/specs/config-import-reverse-fetch.md](docs/specs/config-import-reverse-fetch.md)）：在 FR-38「正向」上传导入之外做「反向」——运维可在管理台选一台**在线实例**为模板，命令其 agent 读取**当前真实落盘的 `plugins/` 文本配置**回传，控制面 ingest 入库为**组/实例级**文件树覆盖（复用 FR-38 通道B 整文件覆盖语义），免手工逐个上传。新建 **server→agent 命令通道**：新增 `agent_command` 表（GORM 可移植、`type`/`status` 落 `VARCHAR`、`payload`/`result_detail` 落 `TEXT`、零方言，真源属 DB——命令需持久/可审计/跨 SSE 断连重拉），命令生命周期 `pending → fetched → done/failed/expired` 经 CAS 迁移；复用 FR-24 SSE 流的 `command-pending` 事件（[ADR-0015](docs/adr/0015-sse-server-push-transport.md) 此前预留、现启用）唤醒目标 agent，agent 经 HTTP 拉命令详情、执行、回传。新增端点：admin `POST /admin/v1/instances/{serverId}/reverse-fetch?namespace=`（写操作 readonly→403，先校验目标在线→建 pending 命令 + `file.reverse-fetch` 审计→发 SSE，返回 202 命令视图）、agent `GET /beacon/v1/agent/commands`（拉最早待办并 CAS 标 fetched，无则 204）、agent `POST /beacon/v1/agent/files/ingest`（回传文件集，控制面入库前再校验后复用 `Import` 落覆盖）。**安全**：agent 读取限死真实 plugins 根内（`toRealPath` 容器校验 + 拒符号链接逃逸 + 纯字符串 path 守卫）、排除 `.jar`/二进制（NUL 字节 + 严格 UTF-8 探测）、单文件 1MB / 总量 64MB / 数量 2000 上限且**整批失败不留半截**，**agent 与控制面双校验**（控制面 `validateIngestFiles` 同口径再挡一道）；越界 path / jar → `400 INVALID_PATH`、超数量 → `422 TOO_MANY_FILES`、超总量 → `422 CONTENT_TOO_LARGE`、空集 → `400 INVALID_PARAM`（命令转 failed）。读盘只在 agent async 适配器线程、core 保持 TabooLib-free 且只经 `HttpTransport`/`JsonCodec` 抽象触达 HTTP/JSON（ADR-0005）。管理台配置中心新增「反向抓取」入口（选在线实例源 + 目标层/组/实例），触发即建命令、结果经命令状态/审计/文件树体现。读取限 plugins 内的文本配置事实、不读 jar/不碰玩家数据，守控制面只存事实边界。
- 管理面只读角色 + 运行时 API 密钥 + 密钥操作审计（FR-42）：引入 full（读写）/ readonly（只读）两级角色，外部服务可用一把**只读**密钥调 `/admin/v1/*` 读取拓扑/实例/zone 等事实、对任何写端点一律 403（"只读拒写"由鉴权中间件 `readonlyWriteGuard` 统一裁决，handler 不碰角色）。新增 `api_key` 表（GORM 可移植、**只存 SHA-256 哈希**、软删即吊销）、`internal/apikey` 叶子包（明文 `bk_` 前缀生成 + 哈希纯函数）、`/admin/v1/api-keys` 端点（创建/列出/吊销/重置）与管理台"密钥管理"页；密钥经独立头 `X-Beacon-Api-Key` 或 `Authorization: Bearer <bk_...>` 认证。明文仅创建/重置时一次性返回、**不可二次读取**（丢失只能重置轮换）；创建/吊销/重置写入既有 `audit_log`（operator=apikey:<名称>，明文不入 detail），复用审计页过滤可查。补充 [ADR-0009](docs/adr/0009-control-plane-auth-pulled-forward.md)（运行时密钥落库属新决策），不引 Redis/会话存储/鉴权框架（见 [ADR-0026](docs/adr/0026-runtime-api-keys-and-readonly-role.md) 与 [docs/specs/admin-readonly-role-and-api-keys.md](docs/specs/admin-readonly-role-and-api-keys.md)）。

### 变更
- 控制面 Go 模块路径由 `beacon` 改为 `github.com/wcpe/Beacon`（对齐仓库地址与 Go 社区惯例，便于将来被外部 import、规避与公共 `beacon` 模块潜在重名）：纯内部重构、运行时行为不变，仅源码 import 路径与构建版本注入路径（Makefile `VERSION_PKG` 与 release.yml ldflags `-X`）随之更新。

### 修复
- 文件树托管 / 配置导入硬化：拒绝 agent 自身 dataFolder 顶段（增强 FR-14 / FR-38）。两道闸闭环——控制面 `normalizePath` 把顶段为 `BeaconAgent` / `BeaconAgentProxy` 的 path 在 `/files` 与 `/files/import` 入库前拒为 `INVALID_PATH`；agent 侧 `FileTreeApplier` 接受壳层注入的 `protectedSegments`（bukkit 注入 `BeaconAgent`、bungee 注入 `BeaconAgentProxy`，core 不硬编码守 [ADR-0005](docs/adr/0005-agent-transport-codec-abstraction.md)），顶段命中即跳过该 path 不取/不写/不删并 WARN（守 [ADR-0010](docs/adr/0010-file-tree-hosting-blob-channel.md) 决策5 fail-static）。与 [FR-41](docs/PRD.md) env 注入身份相辅相成：env 兜身份事实跟机器走、保护路径兜本地 dataFolder 不被外部接管——一份 `plugins/` 同步整 zone 时，agent 自身 config / 快照不会被污染。严格顶段相等，`BeaconAgentX/foo` 不命中（详见 [docs/specs/file-tree-hosting.md](docs/specs/file-tree-hosting.md) §3.1）。

## 0.6.0（2026-06-21）

### 新增
- BC 代理专属指标与角色分流展示（FR-34，扩展 [ADR-0023](docs/adr/0023-control-plane-observability-dashboard.md)，见 [ADR-0025](docs/adr/0025-bc-proxy-metrics-and-netty-traffic.md)）：bc（bungee）agent 新增采集代理专属负载指标——在线连接数、JVM 线程数、运行时长、后端子服可达性（up/total）+ 到各后端平均 ping 延迟（`ServerInfo.ping` 异步探活，在 agent async 上报线程内有界等待汇总、绝不碰 MC 主线程），经 report 新增可选 `proxy` 子对象上报（仅 bc 填、bukkit/旧 agent 缺键向后兼容）。控制面 `runtime.Instance` 加 BC 字段（仅展示不参与决策），`metric_sample` 加 6 个可空列（`proxy_conn`/`thread_count`/`uptime_ms`/`backend_up`/`backend_total`/`backend_avg_latency_ms`，全基础类型 + `NOT NULL DEFAULT`、零方言、AutoMigrate 加列对既有行兼容、可切 Postgres），采样器一并落库；`GET /admin/v1/metrics/summary` 新增 `bc` 子对象（仅 `role=bungee` 聚合：代理数/连接/平均线程/后端可达性/平均延迟），既有 bukkit 聚合零改动无回归。管理台 Dashboard 新增「BC 代理」面板按角色分流展示上述 BC 指标，bukkit 视图不受影响。**网络吞吐入/出字节本期不采**——BungeeCord 公开 API 无干净 Netty pipeline 注入点（吞吐计数只能切进非公开实现 jar `PipelineUtils`），不做脆弱反射 hack、不引 BungeeCord 实现 jar、不留占位列/字段，标「待真机/待定」（详见 ADR-0025）。只采负载计数事实、不采玩家名单/身份（守看人边界 ADR-0022）。
- bc 后端归属事实上报：bc（bungee）agent 将其当前代理的后端子服 serverId 集合（取自 `ProxyServerDirectory`）经 register / report 附加可选 `backends` 字段上报，控制面存为实例只读内存事实（仅 bc 填、bukkit 恒空、旧 agent 缺键向后兼容），实例视图输出 `backends` 供集群拓扑 bc→bukkit 连线消费；只存事实不落 DB、不据它做调度决策（FR-36，见 ADR-0024）。
- 补齐 web 写操作审计（FR-7/FR-30）：新建环境（`namespace.create`）与管理面登录 / 登出（`auth.login` / `auth.logout`）此前未埋审计，现各产一条审计行——环境写入与审计同事务原子完成，operator 取登录认证身份，审计 `detail` 仅记必要字段、严禁含口令 / 令牌。新增登出端点 `POST /admin/v1/auth/logout`（令牌无状态不可吊销，登出仅留审计痕迹），管理台登出按钮先记审计再清本地登录态。
- 审计查询管理台新增「操作人」过滤输入（后端 `GET /admin/v1/audits` 的 `operator` 参数早已支持，FR-30），可按操作者检索审计。
- 控制面自身状态页眉（FR-33）：新增 `GET /admin/v1/system/status`（版本 / 运行时长 / DB 连通 / 在线实例数 / 采样器状态 + Go 运行时资源 goroutine·堆内存 + 进程 CPU%），管理台顶部新增页眉区实时展示控制面进程自身健康，区别于 FR-32 的 agent 网络聚合指标。DB 断开时端点仍返回 200 并反映为「已断开」；进程 CPU% 经 `gopsutil` 采集本进程占用（启动时预热基线、占比钳到 [0,100]），采集失败时优雅降级为「不可用」。新增后端依赖 `github.com/shirou/gopsutil/v4`。
- agent 配置环境变量覆盖（FR-41）：agent（数据面）配置读取新增一层环境变量覆盖（env 优先于 config.yml），变量名约定 BEACON_AGENT_ + 点分路径大写、点与连字符转下划线（如 identity.server-id → BEACON_AGENT_IDENTITY_SERVER_ID），覆盖全部标量与列表配置项（identity.metadata 动态键 map 暂不支持）。core 新增 `EnvOverridingConfigReader` 装饰器（env 以函数注入、不依赖具体环境读取、守 TabooLib-free），双端壳接线；E2E 改以 env 注入 agent 接入信息、删除手写 config.yml 生成。支持容器化用环境变量注入接入信息（见 [docs/specs/agent-config-env-override.md](docs/specs/agent-config-env-override.md)）。
- 配置导入（上传通道，FR-38）：新增 `POST /admin/v1/files/import`（admin 鉴权，`multipart/form-data`），运维可从管理台把一份本地目录 / 多文件批量上传到指定环境的某个「组」，落为组级（`scope=group`）文件树并全局复用，复用 FR-14 通道B 整文件覆盖语义（同 path 已存在则发布新版本、否则首发）。多文件在同一事务内原子落地、提交后唤醒文件长轮询，并记一条 `file.import` 审计（含目标组与文件数）；校验单文件 / 单次总字节 / 文件数上限与相对路径安全（拒绝绝对路径 / `..` 穿越 / 反斜杠）。管理台配置中心顶部新增「导入到组」入口：选环境 + 组、选目录（`webkitdirectory`）或多文件上传，成功后提示并刷新文件树。沿用 ADR-0010，无新 ADR。
- 集群拓扑可视化（FR-37，依赖 FR-36）：新增 `GET /admin/v1/topology?namespace=`（读内存注册表快照，返回节点 + bc→bukkit 真实连线 + 大区/zone 分组；只连当前在册可用的后端、不画悬挂边），管理台新增独立「集群拓扑」页用 ECharts graph 画 bc→bukkit 连线、按角色 / 大区 / zone 区分聚合、节点带在线状态色、React Query 轮询刷新；实例与健康页加角色徽标区分 BC 代理 / 子服。控制面只展示事实、不据它做调度决策、不落 DB。新增前端运行期依赖 `echarts`。

### 变更
- agent E2E 服务端/代理的下载与启动改用 jpenilla run-task（run-paper / run-waterfall 2.3.1，兼容当前 Gradle 8.5）：移除两处手写的 `PrepareMinecraftServerEnvTask` 下载 + `JavaExec` 启动任务，改由 run-paper 的 `runServer`（Paper）与自定义命名的 `RunWaterfall` 任务 `runBungee`（Waterfall）负责下载与运行；MC 版本经 `minecraftVersion` / `waterfallVersion` 指定（取代硬编码直链），可经 `-Pe2ePaperVersion` / `-Pe2eWaterfallVersion` 覆盖。任务名（`runServer` / `runBungee`）与 Go E2E 驱动入口不变。
- 管理台新建/复制配置流程改善（FR-40，增强 FR-1/FR-22）：新建配置对话框的环境/大区/小区/实例选项改为从 API 动态获取（环境取 `listNamespaces`、大区与小区由 zone 汇总与实例列表派生、实例取 `listInstances`），去掉原硬编码示例（prod、__GLOBAL__/server-a/server-b 等）；覆盖目标随覆盖层联动——`global` 隐藏覆盖目标，`group`/`zone`/`server` 切换为对应动态下拉，减少手填出错；配置编辑器新增「复制到实例」动作，把当前配置复制为某实例的 server 层覆盖（预填源内容、覆盖层定为 server、目标待选），进入编辑改 diff 后发布，复用既有创建/发布 API，优先级由既有覆盖链（实例>分组>全局）保证。仅前端改动，无后端新增端点。
- zone 分配页改为看板式归派（FR-35，纯 UI 增强 FR-8）：左侧未指派 server 卡片池 + 右侧按大区分桶的 zone 容器，拖卡到 zone 即指派、跨桶拖拽即改派、拖回未指派池即取消指派；卡片显示 serverId + 角色徽标（子服/BC）+ 在线状态点。复用既有 `PUT/DELETE /zones/assignments`，后端零改动；引入前端拖放库 `@dnd-kit`，onDragEnd 落点解析抽为可单测纯函数。
- agent `BeaconApiClient` 暴露连接失败原因：原 `exec()` 把连接级异常一律吞为 null，`AgentLifecycle` 只能拿到笼统「连接失败」四字日志，stale 旧 jar / DNS / TLS 等真因被诊断黑盒。现 catch 时把「类名: 消息」记入 ThreadLocal（per-thread 隔离，根除 5 条循环并发串台），5 处 `Failed("连接失败")` 改读 `connectFailReason()`；外行为不变（重试节奏、返回类型同），仅日志/Failed.reason 文案更具体。

### 修复
- 代理（bungee）侧跨服消息只起了玩家位置名册、未起完整消息模块，致代理无法收发业务消息（FR-26，首次真机暴露）：`BeaconAgentBungee` 仅装配 `BungeePlayerRosterBootstrap`（维护 `beacon:player-loc` 供子服按玩家寻址），未像子服那样起 `MessagingModule`——故代理不消费自身收件流 `beacon:msg:{proxyId}`、不分发 `on(type)` 处理器、`messaging().isAvailable()` 恒 false（真机实测：代理收件流堆积 6 条、无消费组）。这让所有依赖「代理接收消息」的下游能力失效：跨服踢出 / 跨服广播 fan-out / 跨服切服 / 跨服传送编排——子服把 `send(proxyId, …)` 投到代理收件流后无人消费。修复：新增 `BungeeMessagingBootstrap`（镜像 `BukkitMessagingBootstrap`）在代理启动完整消息模块（消费收件流 + `on` 分发 + RPC 回信 + 主题 `publish`/`subscribe`），接进 `BeaconAgentBungee` 的 enable / 配置变更重连 / disable；与玩家名册引导各持独立 Redis 连接、互不影响、与配置同步故障域隔离，启动失败仅降级。经真机集群（双 Paper 子服 + Waterfall 代理 + 真 Redis + mineflayer bot）验证：代理消息模块干净启动、收件流被消费，跨服踢出 / 广播 / 切服 / 坐标传送 / 传送后 kether 的玩家流程端到端全通。
- 跨服消息中间件启用后连真实 Redis 崩 `NoClassDefFoundError: org/apache/commons/pool2/impl/GenericObjectPoolConfig`（FR-26，首次真机 / 真 Redis 验证暴露的隐藏缺陷）：根因是 jedis 的 `@RuntimeDependency` 只 relocate 了 `redis.clients.jedis`，未把 jedis 内部引用、且本工程同样 relocate 的传递依赖 `org.apache.commons.pool2` / `com.google.gson` 一并声明 relocate。TabooLib 的 relocate 按依赖各自的 jar 生效，故运行期下载并重定位后的 jedis 仍引用原始包名 `org.apache.commons.pool2.*` / `com.google.gson.*`，而类路径只有重定位副本（`top.wcpe.beacon.agent.lib.*`）——`JedisPoolConfig` 加载其父类 `GenericObjectPoolConfig` 即抛 `NoClassDefFoundError`，致 bukkit 消息模块降级、bungee 玩家位置名册初始化失败，整条跨服消息链路不可用（此前单测用未重定位的 jedis 跑、打包 jar 的运行期重定位无覆盖，故绿却实机错）。修复：把 commons-pool2 / gson 的 relocate 规则补进 jedis 这条 `@RuntimeDependency`（`BeaconAgentBukkit` / `BeaconAgentBungee` 双端），使重定位后的 jedis 引用对齐隔离命名空间；slf4j 由平台（Paper / Bungee）提供，保持原始包名、不重定位。经真机集群（双 Paper 子服 + Waterfall 代理 + 真 Redis）验证：两端跨服消息模块与 proxy 玩家位置名册均干净启动、无类缺失。
- 指标看板历史趋势空环境查询报「参数错误」（FR-32）：趋势端点原强制 `namespace` 必填，管理台留空环境（聚合全部）时被后端拒为 `400 INVALID_PARAM`，历史趋势图报错。现 `namespace` 改为可选——为空时聚合全部环境的样本（与 summary 行为一致），repository 趋势查询 `namespace` 为空则不加该过滤。
- 指标看板平均 TPS / 平均 CPU 被 BC（bungee）拉低（FR-32）：聚合原对所有在线实例一视同仁，bungee 作纯代理 tps 恒为 0，计入分母致平均 TPS·CPU 失真偏低。现这两个平均**仅统计 `role=bukkit`**（总玩家数 / 在线服数 / 平均内存仍计全部）；`metric_sample` 新增 `role` 列（VARCHAR，零方言），采样器从注册表 `Instance.Role` 落库，趋势降采样据此排除 bungee。
- agent `/beacon resync` 命令空转（FR-17）：原 resync 子命令无视文件树托管是否启用，一律回占位文案「文件树子系统未启用」且不触发任何同步——文件树托管（FR-14）落地后该命令始终失效。现 `AgentLifecycle` 新增 `forceSyncFileTreeNow()`（与 `forcePollNow` 同形：以空 `fileTreeMd5` 旁路文件树长轮询 304、强制立刻重拉一次清单并由 `FileTreeApplier` 幂等差分落盘，不接管长轮询主循环、不阻塞 MC 主线程），双端壳 `/beacon resync` 改为调用之：文件树子系统启用时回「已触发文件树重新同步」并真正落盘，未启用时回提示开启 `file-tree.enabled`。

## 0.5.0（2026-06-20）

### 新增
- agent-api Discovery 门面新增玩家位置名册只读查询 roster() / rosterInZone(group, zone)，把 agent 侧 Redis 名册（谁在哪个服）只读暴露给业务插件，供跨服看人/人数/补全；控制面零改动、不连 Redis（FR-31，扩展 ADR-0016，见 ADR-0022）。
- 新增控制面可观测看板：补齐 agent 内存/CPU 采集与人数/TPS 真值上报，时序落 MySQL `metric_sample`（按间隔采样 + 保留期清理），新增指标聚合与历史趋势端点，管理台新增 Dashboard 页与趋势图；图表库引入前端运行期依赖 `recharts`（FR-32，见 ADR-0023）。
- 前端单元测试栈（FR-18 管理台）：引入 vitest + @testing-library/react + jsdom，新增 `DataTable` / `AsyncSection` / `StatusBadge` 组件单测与 `ConfigsPage` 关键路径（打开标签 / 切换视图 / Diff / 保存）冒烟测试，作为前端重构的回归安全网。`pnpm test` 运行；测试文件经 `tsconfig` 排除出生产 `tsc -b`，不影响 `go:embed` 内嵌构建。运行方式见 [docs/OPERATIONS.md](docs/OPERATIONS.md) §8。
- 控制面版本注入与启动日志暴露：版本号唯一源为仓库根 `VERSION`，经 `-ldflags -X` 注入控制面、运行时与启动日志可见，三组件（控制面 + 双 agent）版本恒一致（[ADR-0007](docs/adr/0007-versioning-and-release-channels.md)）。
- 工程化：新增 `Makefile` 一键打包（控制面单二进制内嵌前端 + 双 agent jar），CI 门禁（PR 跑控制面 `go build`/`go test` + golangci-lint/gofmt + agent 单测 + 前端构建）与 tag 触发的多平台原生 Release 工作流。

### 变更
- 管理台前端结构重构（纯结构，接口 / 列 / 交互 / 文案不变）：① 抽出 `DataTable`（列定义驱动表格 + 空态）与 `AsyncSection`（加载 / 错误三态）两个公共原语，环境 / 实例 / zone / 审计 / 文件 / 覆盖集六个列表页改用之，消除各页重复的表格骨架与状态分支；② 配置中心 `ConfigsPage` 从 779 行巨石组件拆为 `pages/configs/` 下编排层 + `useConfigTabs`（标签状态 hook）+ `ConfigTabBar` / `ConfigEditorPane` / `EffectivePreview` / `RevisionHistory` / `CreateConfigDialog` / `ConfigFileTree` / `TargetSelector` 等聚焦组件，并删除一直无人引用的死文件 `web/src/components/FileTree.tsx`。
- 管理台列表页「加载失败」提示统一移入卡片内（原部分页显示在卡片上方），并消除「加载失败」与「暂无数据」同时出现的双显（文件树托管页因平铺 / 文件树双视图共享错误态，保持原位）。

### 移除
- 删除已下线且不可达的管理台页面 `FilesPage` / `OverrideSetsPage` / `FileDetailPage`：文件树托管 / 文件覆盖集在 v0.3.0 已合入配置中心，路由重定向到 `/configs`、导航无入口、组件无任何引用方，属死代码。

### 修复
- 管理台「生效预览」整页崩溃（FR-22）：前端原读取后端并不存在的**顶层 `deletions`** 字段（后端实际按 dataId 维度在各 item 内返回 `sources` / `deletions`，见 `effectiveConfigItemView`），`undefined.length` 致 React 整页白屏。现 `EffectivePreview` 按 item 渲染来源与被删除的键、删除顶层读取，`EffectiveConfigView` / `EffectiveConfigItem` 类型对齐后端契约，并补 `EffectivePreview` 单测锁定。此前 dev 因 mock 未覆盖 effective deletions 而未暴露。
- 配置保存后历史修订面板不即时刷新：保存成功仅失效配置列表缓存、未失效历史修订查询，新版本需重开标签才出现；现保存后一并失效 `config-revisions`，历史面板即时刷新出新版本。
- 首启 `.env` 自动生成覆盖 `config.yml`（FR-25）：原首启在释放 `config.yml` 之外还自动生成一份 `.env`，而该 `.env` 模板含 `BEACON_HTTP_ADDR` / `BEACON_BOOTSTRAP_TOKEN` / `BEACON_ADMIN_USERNAME` 等**与 `config.yml` 重叠的字段**；因生效优先级 `.env` > `config.yml`，运维改了 `config.yml` 的这些项重启却不生效，被 `.env` 静默盖掉。现**不再自动生成 `.env`**：首启释放 `config.yml` 时用 `crypto/rand` 把留空的 `auth.password` / `auth.secret` **就地填入随机强值**（文件权限 0600、口令不入日志），`config.yml` 即开箱真源、无固定弱默认口令、仍开箱即跑（不 fail-fast）。`.env` 加载机制保留：运维手动放置的 `.env` 与真实环境变量仍按 `真实 env > .env > config.yml` 覆盖。鉴权强度不变（[ADR-0009](docs/adr/0009-control-plane-auth-pulled-forward.md)）。
- agent 默认 `bootstrap-token` 与控制面 `agent-token` 不一致致注册被拒：agent 两端（bukkit/bungee）默认令牌为 `beacon-bootstrap-2026`，而控制面模板 `config.example.yml` 与文档均为 `beacon-bootstrap-token`，开箱默认值对不上，agent 注册时 `X-Beacon-Token` 校验失败（401）。现把 agent 两端默认 `bootstrap-token` 对齐为 `beacon-bootstrap-token`，与控制面默认一致、开箱即连（该令牌仅防误连、非安全边界，[ADR-0009](docs/adr/0009-control-plane-auth-pulled-forward.md)）。

## 0.4.1（2026-06-19）

### 修复
- 配置发布前 schema 校验（FR-27）：YAML 非字符串顶层键（如 `1: a`、`2024-01-01: x`、`true: y`）原误判为"顶层必须是键值映射，不能是标量或列表"——消息事实错误（它本就是 map），现识别 yaml.v3 解析出的 `map[any]any` 并返回准确语义错误 `CONTENT_SCHEMA_INVALID`（配置键必须是字符串），仍属拒绝（merge 全链路只处理字符串键）。同时空键判定改用 `strings.TrimSpace`，覆盖全 Unicode 空白（原自实现仅识别 4 种空白）。
- 配置发布 / 回滚并发撞唯一键（`uk_revision_version`，并发发布同一目标版本）原透出 `gorm.ErrDuplicatedKey` 致 500，现映射为 409 `CONFIG_CONFLICT`（FR-9）。
- 灰度并发发布同一配置项死锁修复（FR-9）：「先软删后建」灰度发布事务在 InnoDB 高并发下会瞬时死锁（1213）/ 撞唯一键而透出 500。改用 `config_item.gray_version` 乐观锁——发布前以该版本做 CAS（仅当未被并发改动才 +1），抢到的那路才进「先软删后建」段、未抢到的重读版本重试；CAS 在 item 行上串行化（单行锁、无环），从源头消除 `uk_gray_item` 上的死锁。各路重试后最终都成功、恰留一条活跃灰度（重发即覆盖），不再以 409/500 拒绝（移除 `GRAY_CONFLICT`）。新增 `config_item.gray_version` 列（内部序列化令牌、不外泄、GORM 零方言）。
- 灰度 promote 走发布路径却未自增 `beacon_config_publish_total`（指标偏低，FR-30）：`ConfigGrayService` 注入 `PublishRecorder`（同 `ConfigService.SetMetrics` 模式，可选注入），promote 成功后计入发布计数。
- 跨服消息玩家名册退出泄漏（FR-26）：玩家整体断开时 Bungee `player.server` 为空 → 退出来源服为空串 → `shouldDeleteOnQuit` 对空串恒为 false → 名册项永不删、Redis `beacon:player-loc` 泄漏。现来源服为空时无条件删该玩家名册项（玩家已离线、无换服误删风险），非空仍按名册当前值比对（换服误删保护不变）。

## 0.4.0（2026-06-19）

### 新增
- 跨平台 E2E 编排 + CI 工作流：端到端验收从 Windows-only PowerShell 编排（`run-override-e2e.ps1` / `run-directory-e2e.ps1`，已删除）迁到**纯 Go、真跨平台**入口 `go test -tags=e2e -timeout=30m ./test/e2e/{override,directory}`（Windows/Linux/macOS 一致、由测试自管控制面 + 真 Paper/Waterfall 生命周期，`--no-daemon` + 进程树整体击杀，免按端口杀）；必填 `E2E_ADMIN_PASS`/`E2E_AUTH_SECRET`，可选 `E2E_DB_DRIVER`（默认 `sqlite`，无需 docker/MySQL；可选 `mysql` 配 `E2E_DB_DSN`）/`E2E_BEACON_URL`（默认 `http://localhost:8848`）。新增 GitHub Actions：`.github/workflows/ci.yml`（PR/push 轻量门禁——控制面 `go build`+`go test`、golangci-lint+gofmt/goimports、agent 单测、前端 pnpm build）与 `.github/workflows/e2e.yml`（手动 + nightly 触发，ubuntu 默认 sqlite 跑 directory/override 两条 E2E，口令/密钥经 secrets 或运行时随机注入、不硬编码）。在 Windows 真机 JDK21 + 真 Paper 1.20.4 + 真 Waterfall 上 directory/override 两套 E2E 全相位绿。运行方式见 [docs/OPERATIONS.md](docs/OPERATIONS.md) §7。
- 控制面首启脚手架 + .env 加载（FR-25，见 [docs/specs/control-plane-bootstrap-scaffold.md](docs/specs/control-plane-bootstrap-scaffold.md)）：单二进制首次启动自动把配置模板（`config.yml`，默认 sqlite 可直接跑）释放到当前目录、并在无 `.env` 时**生成 `.env`（0600）随即直接启动**（开箱即跑、不再 fail-fast）：`BEACON_ADMIN_PASSWORD`/`BEACON_AUTH_SECRET` 用 `crypto/rand` 随机（口令写入 `.env`、用户打开即见、可改，**不输出到日志**），`BEACON_BOOTSTRAP_TOKEN` 用固定默认 `beacon-bootstrap-token`（仅防误连、非安全边界，与 agent 样例开箱匹配，免逐机同步随机值）。并从 `.env` 加载环境变量（仅填补未设置项、真实 env 优先），免手工 `export`。鉴权仍强制——口令/密钥强随机 + env 注入、不入库、不用固定弱默认口令，不弱化 [ADR-0009](docs/adr/0009-control-plane-auth-pulled-forward.md)。默认配置文件改用 `.yml` 扩展名（对齐 `.claude/rules/config-files.md`），`config.example.yaml` 随之更名 `config.example.yml` 并补 `driver`、默认 sqlite（修正 v0.3.0 后遗留的 MySQL-only 模板漂移）。手写最小 `.env` 解析、不引第三方库；config 模板经根包 `//go:embed` 内嵌。生成的 `config.yml` 为运行时产物、经 `.gitignore` 排除不入库。新增 `internal/config` 的 `LoadDotEnv`/`EnsureFile`/`EnsureBootstrapEnv` 单测，并经空目录真机冒烟（首跑生成 `.env`+`config.yml` 并直接启动 sqlite、用生成口令登录 200、agent 端点带默认 token 不被 401、错 token 仍 401、已存在不覆盖、真实 env 优先）。
- Proxy 服务器目录注入（FR-4 服务发现延伸出口）：BeaconAgentProxy 注册成功后周期调用 discovery 同步同 namespace 在线 `role=bukkit` 子服，按 `serverId` 注入 Bungee `ServerInfo` 目录（仅管理 Beacon 创建的条目，同名手工配置 WARN 跳过不覆盖）；控制面失联时按本地已注入目录继续（fail-static）。配套 headless 真机 E2E（见 [docs/OPERATIONS.md](docs/OPERATIONS.md) §7.2）：新增 `agent-e2e-bungee` 的 `DirectoryE2EProbe`（周期快照 Bungee `ServerInfo` 目录与 `beacon` 命令注册态）、`test/e2e/directory` 跨平台 Go E2E（`go test -tags=e2e`），控制面用 SQLite（免 Docker）。在真 Paper 1.20.4 + 真 Waterfall 上两相位全绿：目录按 serverId 注入 + 手工服保留 + 命令注册、杀控制面后已注入目录不清空（fail-static）。
- agent↔控制面 server→agent 推送合并为单条 SSE 流（FR-24，见 [ADR-0015](docs/adr/0015-sse-server-push-transport.md) 与 [docs/specs/sse-server-push-transport.md](docs/specs/sse-server-push-transport.md)，取代 [ADR-0006](docs/adr/0006-rest-long-poll-push.md)、扩展 [ADR-0005](docs/adr/0005-agent-transport-codec-abstraction.md)）：控制面新增 `GET /beacon/v1/agent/stream`（`text/event-stream`，held-open）把 配置/文件树/覆盖集 三条长轮询合并为一条推送流——**只发变更通知**（`config-changed`/`file-changed`/`override-changed` + 新 md5，agent 收到用现有端点取内容并应用，blob 仍走 HTTP）、**连接即对账**（agent 上报各通道 md5 → 控制面比对当前 md5 对落后通道补发增量 → 发 `ready` → 转直播，复用长轮询「先注册后算」+「最小受影响 serverId 集合」唤醒，不丢更新）、无变更发 SSE 注释行（`: ping`）保活并带 `X-Accel-Buffering: no` 关反代缓冲。agent 侧扩展 ADR-0005 新增 `StreamTransport`/`StreamEvent` 端口 + 纯逻辑 `SseFrameParser`（按空行分帧、注释行心跳忽略），SSE 客户端 `OkHttpStreamTransport`（纯 HTTP 读流、无 netty/无重型件）只在适配器；`AgentLifecycle` 注入 `streamTransport` 时以单条 SSE 流循环取代三条长轮询循环（未注入则退回长轮询，迁移期兼容）、流断退避重连且重连即对账。**健康判活仍由独立心跳 + TTL 决定、与流活性解耦**，fail-static 不变。新增控制面 `internal/sse` 事件编码与 `DiffEvents`/`StreamService.Run` 单测（sqlite-backed 跑通连接即对账 + 直播热推 + 只发受影响）、SSE 集成测试（`-tags=integration`），agent `SseFrameParser`/`ChangedPayload`/`AgentLifecycleStream` 单测（启用 SSE 取代长轮询 + 对账 URL 携带 md5 + config-changed 取数据 apply + 流断重连）全绿；双端 build 全绿。远程命令与 [FR-29](docs/PRD.md) watch 作为消费者复用本流（各自独立 FR）。
- 配置发布前 schema/类型校验（FR-27，增强 FR-1/FR-3，见 [docs/specs/config-publish-schema-validation.md](docs/specs/config-publish-schema-validation.md)）：发布前（`Create`/`Publish`/`Rollback` 三条写路径）在既有「格式/大小/可解析」之后追加结构与类型校验——非空内容顶层必须是键值映射（拒裸标量/列表根，否则深合并会整体冲掉其它覆盖层、下发后目标服 apply 异常）、所有键须非空（递归进嵌套 map），不通过则拒绝发布并返回 `422 CONTENT_SCHEMA_INVALID`。校验为 `merge.ValidateSchema` 无副作用纯函数、由 service 层 `validateContent` 统一接入，handler 不碰校验细节、错误走统一 apperr。规则刻意保守，不引入按 dataId 的字段级 schema 注册表（需要时另立 FR）。穷举单测覆盖合法通过 / 顶层标量·列表被拦 / 顶层及嵌套空键被拦 / 空内容放行（merge 层纯函数单测 + service 层错误码映射白盒单测）。
- 健康分级 + 失联告警（FR-28，增强 FR-5，见 [ADR-0019](docs/adr/0019-health-alert-channel-abstraction.md) 与 [docs/specs/health-grading-and-alerting.md](docs/specs/health-grading-and-alerting.md)）：健康状态机在 `online → lost → offline` 之间引入 **degraded（亚健康）**，按心跳陈旧度推进 `online → degraded → lost → offline`（阈值 `degraded-after-sec < ttl-sec < offline-grace-sec` 可配，启动校验序关系、错序 fail-fast）；实例**进入异常态**（degraded/lost/offline）时主动告警、恢复 online 不告警。告警出口抽象为 `Alerter` 接口 + `Dispatcher` 扇出（逐通道兜错、某通道失败仅 WARN、不阻断健康扫描），第一版实现**站内信**（进程内环形缓存、独立锁不嵌套、`GET /admin/v1/alerts` 只读、重启清零）与 **webhook**（配置 `url` 非空才挂载、HTTP POST 告警 JSON、IO 在扫描循环不持注册表锁），新增通道只实现 `Alerter` 接入；告警不落库（健康事件派生，与"注册/健康真源在内存"一致）。穷举单测覆盖状态机 online→degraded→lost→offline 全程流转 + 任意异常态心跳恢复、告警仅异常态触发 + 多通道扇出兜错 + 站内信环形容量/快照隔离 + webhook 投递 JSON + 阈值序关系校验。
- 流量调度：落位均衡 + drain 标记（FR-10，P2，见 [ADR-0017](docs/adr/0017-traffic-scheduling-decision-vs-execution.md) 与 [docs/specs/traffic-scheduling-placement-drain.md](docs/specs/traffic-scheduling-placement-drain.md)）：控制面**只给调度决策（query-only），不执行玩家连接**。新增 admin API `GET /admin/v1/scheduling/placement?namespace=&group=&zone=`（某 zone 内落位候选，仅纳入 `online` 且未 drain 的实例、按 `weight` 降序→`capacity` 降序→`serverId` 升序确定性排序，空集/全 drain/全离线返回空候选不报错；**不读** agent 上报 `playerCount`/`tps`）与 drain 标记 `GET/PUT/DELETE /admin/v1/scheduling/drains`（drain 是运维决策，落 DB `server_drain` 表、与 zone 指派同软删模式、事务内写表+审计原子完成，跨控制面重启存活）。落位排序为无副作用纯函数 `RankPlacement`，穷举单测覆盖排序/drain 剔除/非在线剔除/空集/全忙边界；DB 侧 drain CRUD + 落位联动经集成测试（`-tags=integration` + `BEACON_TEST_DSN`）。**不做 canary 引流**（范围外，见 ADR-0017）；真正送人由数据面执行，控制面不碰玩家连接、不触发长轮询唤醒。
- 可观测性（FR-30，增强 FR-7，见 [ADR-0020](docs/adr/0020-prometheus-metrics-observability.md) 与 [docs/specs/observability-metrics-and-audit-query.md](docs/specs/observability-metrics-and-audit-query.md)）：① 控制面用标准 `prometheus/client_golang` 在同端口暴露免鉴权的 `GET /metrics`（Prometheus 文本，内网信任面），导出 `beacon_instances_registered{namespace,role}`/`beacon_instances_status{status}`（注册数与健康分布，pull 模型——自定义 collector 抓取时读内存注册表实时计数，写路径零侵入）与 `beacon_config_publish_total`/`beacon_push_notify_total`（配置发布与长轮询推送累计 counter，由 service/notifier 经窄接口可选注入自增，未注入即 no-op）；指标集中在新 `internal/metrics` 包持独立 registry、不污染全局 default。② 审计查询 `GET /admin/v1/audits` 新增 `operator` 过滤维度（按操作者检索，与既有 namespace/action/targetType/targetRef/from/to 过滤与分页叠加）。补 `internal/metrics` 单测（指标注册/暴露、注册数 gauge 反映注册表、counter 自增）与 `internal/repository` 审计 operator 过滤 sqlite 单测（红→绿），并补 server/service 集成测试覆盖 `/metrics` 免鉴权抓取与 operator 过滤的 HTTP 链路（DB 侧需 `BEACON_TEST_DSN`，未配则跳过）。
- 敏感配置 at-rest 加密（FR-20，自 FR-11 拆分、FR-26 前置，见 [ADR-0018](docs/adr/0018-config-encryption-at-rest.md) 与 [docs/specs/config-encryption-at-rest.md](docs/specs/config-encryption-at-rest.md)）：新建配置项可传 `sensitive: true`，其 `content` 以 **AES-256-GCM**（标准库 `crypto/aes`+`crypto/cipher`，不引第三方）加密落库——`config_item`/`config_revision` 的 `content` 列存 `enc:v1:` 前缀 base64 密文（TEXT、经 GORM 抽象不绑方言、可切 Postgres）。加解密只在新增 `internal/secret` 叶子包 + 两个配置仓库的写/读边界发生，**service 层始终只见明文**，md5 / scope 覆盖链合并 / 发布前 schema 校验全部基于解密后明文、与非敏感项行为一致；读取详情 / 历史版本 / 有效配置解析自动解密，**下发给 agent 的是明文**（数据面内网可信不变、agent 不持密钥）。密钥仅从 env `BEACON_CONFIG_ENCRYPTION_KEY`（base64 的 32 字节）读取，**绝不入库 / 不入仓 / 不打日志**；库中已有敏感项却未配置密钥 → 控制面 **fail-fast 拒绝启动**。配置项视图新增 `sensitive` 布尔标记，永不回吐密钥 / 密文。**不做** KMS / 信封加密 / 密钥轮换编排 / 按 key 加密（范围克制，仅做支撑 FR-26 的最小闭环）；不弱化 [ADR-0009](docs/adr/0009-control-plane-auth-pulled-forward.md) 鉴权。穷举单测覆盖加解密往返一致 / 错误密钥与篡改密文 GCM 校验失败 / 无密钥 fail-fast / 非敏感项不加密 / 敏感项密文 at-rest 而读出解密 / 加密项 md5 与有效配置解析正确（解密后再算），双端 build 与受影响包测试全绿。
- 配置灰度 / Beta（FR-9，增强 FR-1/FR-3，见 [ADR-0021](docs/adr/0021-config-gray-cohort-version-selection.md) 与 [docs/specs/config-gray-cohort.md](docs/specs/config-gray-cohort.md)）：某 `config_item` 可**先把新内容只灰度给一小撮目标 server**（显式 serverId 名单 cohort），观察无误再 **promote 晋升为全量稳定版**，或 **abort 中止丢弃**。灰度作用在"某 dataId 用哪个版本内容"的**版本选择**层、与 scope 覆盖链**正交叠加**（不新增覆盖层、不改 `merge` 纯函数）——解析时拉完四层候选后按 `ns + 候选项集合`一次性取活跃灰度（无 N+1），对"该项存在灰度且当前 `serverId` 在 cohort 内"的候选替换合并内容；**名单外 `serverId` 解析结果与无灰度时逐字节相同**，admin 预览与 agent 热路径共用同一叠加逻辑。新增 admin API `POST /admin/v1/configs/{id}/gray`（发布灰度）/`POST .../gray/promote`（晋升）/`DELETE .../gray`（中止）/`GET /admin/v1/configs/gray?namespace=`（列活跃灰度）；灰度落 DB `config_gray` 表（cohort 为 JSON 数组文本落 `TEXT`、软删哨兵、零方言、可切 Postgres），一个配置项至多一个活跃灰度（重发即覆盖）。灰度内容同样过发布前 schema 校验（FR-27），灰度项 `sensitive` 与所属项镜像、敏感走 FR-20 at-rest 加密（复用既有仓库加解密边界，service 只见明文）；`promote` 走既有发布路径（进版本历史、可回滚）。三操作均事务内写表 + 审计原子完成，提交后**只唤醒受影响 `serverId`**（发布 / abort 唤醒 cohort、promote 唤醒该项 scope ∪ cohort，复用既有长轮询 / SSE 唤醒集合）。**不做**百分比放量 / 金丝雀引流（属 FR-10，ADR-0017 已划外）/ 自动晋升 / 超时自动回滚（属 FR-12）。cohort 序列化纯函数（去重 / 去空白 / 字典序）单测全绿；灰度发布 / promote / abort / md5 / 校验 / 唤醒命中的集成测试（`-tags=integration` + `BEACON_TEST_DSN`）已就绪，需 MySQL 测试库执行。
- 发现接口过滤 + 拓扑 watch（FR-29，增强 FR-4/FR-16，复用 FR-24 SSE 推送流，见 [docs/specs/discovery-filter-and-topology-watch.md](docs/specs/discovery-filter-and-topology-watch.md)）：① 发现端点 `GET /beacon/v1/agent/discovery` 与 agent-api SDK 新增按自定义元数据 `tag.<key>=<value>` 过滤（可重复、多 tag 取交集，按实例 `metadata` 键值精确匹配，与既有 role/zone/group 维度叠加），`runtime.Filter` 增 `Tags`、`DiscoveryQuery` 增 `tag(k,v)`。② **拓扑 watch**：新增 `topology-changed` SSE 事件类型 + namespace 级唤醒 `topologyHub`，复用 FR-24 单条 SSE 推送流（不另起长轮询、不新开连接）——实例上线/下线（注册 / 手动下线 / 健康转 lost·offline）/ 改派 zone 四处变更点经 `ChangeNotifier.NotifyTopologyChange` 唤醒该 namespace 全部拓扑 waiter，`StreamService` 维护 namespace 拓扑摘要（`runtime.TopologyDigest`：对可用集合 online+degraded 按 `serverId|role|group|zone|status|address` 排序取 md5，运行指标不入摘要）连接即对账 + 直播「真变才推」（摘要未变不推）；事件仅携新摘要、不搬实例数据，agent 收到后重查发现端点取最新拓扑（守控制面/数据面边界）。agent-api `Discovery` 增 `watch(TopologyListener)` 订阅 API（注入推送流时注册到 `TopologyWatchHub`、返回可注销句柄；未启用流的回退态返回不可用 no-op 句柄）。穷举单测覆盖 role/zone/tag 过滤命中与排除（多 tag 交集、缺键排除）、TopologyDigest 幂等与拓扑变化敏感、StreamService 上线/下线/改派推与未变不推、discover tags 拼参、watch 注入态/回退态行为；双端测试全绿。真机/集成（compose 起 beacon+agent+bungee 观察目录随拓扑实时刷新）待真机验。
- agent 内置跨服消息中间件（FR-26，P3，见 [ADR-0016](docs/adr/0016-agent-cross-server-messaging-middleware.md) 与 [docs/specs/cross-server-messaging-middleware.md](docs/specs/cross-server-messaging-middleware.md)）：agent 内**独立可开关模块**（`messaging.enabled` 默认关），与配置同步/心跳**故障域隔离**（独立连接/线程），为业务插件提供与内容无关的通用服务器间传输——四种模式定向发送 / 请求-响应（RPC，关联ID + 超时）/ 主题发布订阅 / 按玩家所在服寻址。`agent-core` 新增 `messaging/`：消息信封 `Message`（type/version/correlationId/replyTo/source，演进「只增不改」）、传输端口 `MessageTransport`（遵循 [ADR-0005](docs/adr/0005-agent-transport-codec-abstraction.md)，core 不 import Jedis）、路由分发 + RPC Future/超时引擎 `MessageBus`、玩家寻址端口 `PlayerLocator`、对外门面 `MessagingView`/降级门面 `DisabledMessaging`/持有者 `MessagingHolder` 与编排 `MessagingModule`；`agent-api` 暴露 `Messaging`（send/call/publish/subscribe/sendToPlayer/on + isAvailable）经 `BeaconAgent.messaging()`。Redis 适配器 `agent-adapters/messaging`（`RedisMessageTransport`：Streams + 每服消费组 + MAXLEN 裁剪做可靠送达离线补消费，pub/sub 做主题与 RPC 回信通道；`RedisPlayerRoster` 维护玩家名册、换服误删保护、重启全量重建）只在适配器、**Jedis 经 TabooLib `@RuntimeDependencies` 运行期下载并 relocate 到隔离命名空间、不 shade、不经 CoreLib**（决策 14）。Redis 连接（host/port/db/password）由 **Beacon 配置中心下发**（决策 15，密码依赖 FR-20 加密先行），冷启动未取得配置时模块保持降级；双端壳层据下发配置启停/重连，proxy 经 Bungee 事件维护玩家名册。仅提供通用传输，**匹配/对战/存储/排行等业务功能不在范围**（属③层业务插件）。纯逻辑全单测覆盖：信封序列化往返、RPC 关联ID 配对与超时、迟到回信容错、四种模式闭环、可靠送达离线补投、降级与 close、玩家寻址解析与落空兜底、换服误删保护、Redis 信道命名与下发配置解析、消息门面/模块装配；双端 build 与受影响模块测试全绿、插件 jar relocate 通过（Jedis 引用已重写到隔离命名空间）。**真 Redis 连接（Streams/pub-sub 收发、离线补消费、RPC 回信、按玩家寻址）与 proxy/Bungee 事件联动待集成/真机验**（本环境无 Redis）。

## 0.3.0（2026-06-18）

### 新增
- 配置有效预览端点（FR-22，见 [ADR-0013](docs/adr/0013-admin-effective-config-preview-and-provenance.md)）：新增只读 `GET /admin/v1/configs/effective?namespace=&serverId=（可选 &group=&zone=）`，预览某目标按 scope 覆盖链合并后的有效配置，与 agent 端 `/beacon/v1/agent/config/effective` 同源、内容与 md5 一致，但不挂长轮询、不强制注册（可预览未注册/假定指派目标）。额外返回**逐键来源层 provenance**（每个叶子键最终来自 global/group/zone/server 哪层 + 被 `null` 减量删除的键），供管理台「服务器视角/文件覆盖矩阵」表达"100 台共用基线 + 某几台增量/减量"。provenance 经新增的 `merge.MergeDataIDWithProvenance` 平行纯函数计算，**不改 `DeepMerge`/`MergeDataID` 这条 agent 合并热路径**，并以"合并结果与 `MergeDataID` 逐一致"的交叉测试守护防双实现漂移；穷举单测覆盖增量来源/减量删除/嵌套/list 整替/删后重加/properties 含点键。落在既有覆盖链上，不引入灰度/流量调度等 P2/P3。
- 配置中心 VS Code 风格编辑器（FR-23，增强 FR-6/FR-18/FR-21）：配置中心页面改为 VS Code 风格——左侧资源管理器树形结构（配置文件 + 实例/分组两级选择器，点击过滤文件树）+ 右侧 Monaco 编辑器（yaml/json/properties 语法高亮、自动缩进、代码折叠、括号匹配、Ctrl+Z 撤销、Ctrl+S 保存）+ 底部历史修订面板（可折叠，点击条目联动 Diff 模式）+ 新增"生效预览"Tab（调用 `GET /admin/v1/configs/effective` 展示合并后配置 + 逐键来源 provenance + 被删除键）+ 保存按钮与 Ctrl+S 全局快捷键；页面整体固定（`h-screen overflow-hidden`），仅编辑器内容可滚动；移除旧文件树托管页和文件覆盖集页（路由重定向到 `/configs`）；新增 Monaco 依赖（`monaco-editor` + `@monaco-editor/react`）和 `gorm.io/driver/sqlite`（后端 SQLite 开发模式支持）
- 后端 SQLite 开发模式支持（见 [ADR-0012](docs/adr/0012-web-shadcn-ui-design-system.md) 相关）：`config.Database` 增加 `Driver` 字段（`mysql`/`sqlite`，默认 `sqlite`），`store/db.go` 根据 driver 选择 GORM Dialector，新增 `BEACON_DB_DRIVER` 环境变量；默认连接池改为 MaxOpenConns=1（SQLite 单连接）
- Mock API 层补齐（`web/src/api/mock/`）：新增 `data.ts`（8 条配置 + 5 个实例 + 3 个 zone 汇总 + 历史/diff 数据）、`handlers.ts`（10 个 API 端点 mock 路由）、`index.ts`（fetch 拦截器，开发模式自动启用）；补齐所有接口 mock（configs CRUD + revisions + diff + effective + instances + zones + files + override-sets + audits + namespaces + auth/login）
- 下游身份就绪等待 SDK 能力 + 身份来源方向反转（见 [ADR-0014](docs/adr/0014-downstream-identity-source-direction.md)）：`agent-api` 的 `BeaconAgent` 新增 `awaitRegistered(timeoutMillis)`（有界等待首次注册成功、zone 已按控制面应答回填——等待判据是「首次注册成功」而非「zone 非空」，未指派 zone 是合法终态；已就绪立即返回 true，超时返回 false，`<=0` 只查当前是否就绪），`agent-core` 以 `CountDownLatch` 在首次 `onRegisterSuccess` 放行实现；`agent-kit` 的 `BeaconAccess` 新增 `awaitIdentity(timeoutMillis)`（在场且就绪→含权威 zone 的身份，不在场或超时→空）。供下游（CoreLib）在启动阶段「优先取 Beacon 的 serverId/zone」用：agent 在场则持续等待至取得确定身份（zone 未指派则中止启动、不兜底），仅 agent 不在场才降级本地 config + 告警（ADR-0014）。据此修订 `docs/SDK.md`「身份走 CoreLib」纪律——普通业务插件身份仍经 CoreLib，仅 CoreLib 自身来源反转（**不推翻** [ADR-0004](docs/adr/0004-zone-authority-control-plane.md) 的 agent 自管身份与控制面 zone 权威）。新增 lifecycle 就绪等待并发单测（就绪前 false / 就绪后 true / 多线程并发等待），`agent-core` build 全绿。

### 变更
- 管理台前端全量改用 shadcn-ui 默认样式（FR-21，增强 FR-6/FR-18，见 [ADR-0012](docs/adr/0012-web-shadcn-ui-design-system.md)）：引入 Tailwind v4 + shadcn-ui（默认 neutral 主题、组件源码入库 `web/src/components/ui/`），统一替换原手写 CSS 的列表/表单/按钮/徽标/对话框；"查看详情"由原列表下方内联展开改为——配置中心 / 文件树托管走**独立路由详情页 + Tabs**（概览/发布/历史/对比，`/configs/:id`、`/files/:id` 仍可深链），文件覆盖集走 **Sheet 抽屉 + 发布 AlertDialog 二次确认**（取代原勾选门控），实例与健康 / 审计日志走 **Dialog**（实例可看完整 metadata、审计可看完整 detail），zone 分配 / 环境管理的表单收进 Dialog；操作反馈由消息条改为 sonner toast，确认操作由 `window.confirm` 改为 AlertDialog。纯 UI/交互改造，所有 API 调用、查询/分页/轮询/长轮询逻辑与行为零回归。

### 修复
- 三方覆盖 / 文件树镜像落盘目录重复 `plugins`（FR-14/FR-15）：TabooLib `getDataFolder()` 在真机返回相对路径（`plugins/<本插件>`）时，`PlatformAdapter.pluginsBaseFolder()` 取到相对 `File("plugins")`、其 `parentFile` 为 null，下游 `serverRoot` 回退成 `plugins` 本身，致 agent 把覆盖 / 文件树文件落到 `plugins/plugins/<目标>` 而非 `plugins/<目标>`——三方插件目录覆盖与文件树镜像在真机实际未落到正确目录（路径限定仍生效、文件仍在 `plugins/` 内，属落错位置非逃逸）。修复：`pluginsBaseFolder()` 先 `absoluteFile` 再取父级，使服务器根稳定解析；并在 `AgentAssembly` 增 fail-closed 守卫——解析出的 plugins 基目录名非 `plugins` 时关闭文件树与覆盖落盘（宁可不落、不落错）。本缺陷由 FR-15 命令执行真机 E2E 暴露。
- 三方覆盖集成员「内容只改、path 不变」时热更不传播（FR-15，关闭本段「已知项」缺口①）：控制面 `OverrideMD5` 指纹公式只含 `name|targetRoot|reloadCommand|memberPaths`、**不含成员内容指纹**，致成员文件仅改内容（path 不变）时 overrideMd5 不变 → agent 长轮询返回 304 → 不重取落盘（连显式对该集 Publish 也不下发）。修复：把每个成员 `file_object` 行上**按字节算定**的 `content_md5`（ADR-0011 决策 9）折入 overrideMd5 计算（新增 `EffectiveOverrideSet.MemberMD5s` 仅参与指纹、**不投递给 agent**，沿用 `fileTreeMd5` 的 `path:md5` 思路），使「内容改了」也改变 overrideMd5、触发 agent 重取落盘；内容不变时 md5 幂等（不无谓重推 / 不重复 reload）。成员指纹经同一 `ListByOverrideSet` 查询取齐、无额外回表。未弱化任一 ADR-0011 约束（禁 shell / Path 级路径限定 / 本地白名单 gate / 回滚不重放命令 / fail-static 不变），经一轮对抗式安全复审（无新攻击面、`MemberMD5s` 不泄露给 agent）。补 filetree 穷举单测（内容指纹敏感 + 幂等）与控制面集成测试（内容编辑后 overrideMd5 变 + override 长轮询被唤醒）。

### 安全
- FR-15 三方覆盖命令执行真机端到端验收落地并跑通取证（关闭 0.2.0「命令执行真机 E2E 未跑」已知项）：扩 `agent/agent-e2e` 验收插件（种原文件 + 注册受限重载命令 + 观测「文件被改 / 命令收到 + 命令时磁盘 md5」）+ `test/e2e/override` 跨平台 Go E2E（`go test -tags=e2e`），在真 Paper 1.20.4 + 控制面上验三条 ADR-0011 关键链路：① 放行白名单下「备份原文件 → 原子覆盖 → 落盘成功后才派发命令」次序（命令收到时磁盘已是覆盖后内容）；② 控制面回滚到无命令版本只还原事实、不重放命令（agent 内部回滚不重放由既有单测守）；③ **默认空白名单不派发任何命令（inert）**，控制面挂掉文件不动、命令不发（fail-static）。过程未弱化任一 ADR-0011 约束，并经一轮对抗式安全复审（路径绝对化修复 + fail-closed 守卫后，逃逸向量穷举无法逃出 `plugins/<plugin>/`、无 shell 派发、无本地白名单绕过）。
- 同批补 FR-14 文件树镜像与 FR-18 管理台前端的真机验收：文件树文件发布后 agent 镜像落盘到插件真实数据目录、验收插件读到镜像内容（与上述路径修复共同确证镜像落盘在真机生效）；管理台经浏览器自检登录 → 文件树页 → 覆盖集详情**发布前 dry-run 只读预览 + 二次确认门控**渲染正常。

### 已知项
- FR-15 覆盖集缺口（不阻断命令执行真机验收，已立工单后续处理）：控制面无成员挂载 admin API（覆盖集只能建空壳、成员仅能数据层写入），E2E 用数据层造成员绕过。（原「成员仅改内容、不变 path 时内容热更不传播」缺口已修，见上「修复」段。）

## 0.2.0（2026-06-17）

> **破坏性变更（pre-1.0，按 [ADR-0007](docs/adr/0007-versioning-and-release-channels.md) 在 0.y.z 内允许、于此标明）**：管理面 `/admin/v1/*`（登录端点除外）自本版本起需 Bearer 登录令牌（FR-11）——此前开放访问的 admin API 调用方须先 `POST /admin/v1/auth/login` 取令牌再调用；agent 侧 `X-Beacon-Token` 语义不变。

### 新增
- 管理面鉴权（FR-11，自 P2 前移，见 [ADR-0009](docs/adr/0009-control-plane-auth-pulled-forward.md)）：新增 `POST /admin/v1/auth/login`（单操作者凭据 → 无状态 HMAC 签名令牌）；`/admin/v1/*`（登录除外）挂登录令牌中间件，缺/错/过期令牌一律 401；写操作 operator 改以认证身份为准写入审计，取代前端手填值；凭据/密钥走 env（`BEACON_ADMIN_USERNAME`/`BEACON_ADMIN_PASSWORD`/`BEACON_AUTH_SECRET`）不落库、不引 Redis；agent 侧共享 token 语义不变。
- agent 本地运维命令（FR-17，仅本地）：双端壳注册 `/beacon`（权限 `beacon.admin`）含 `status`（生命周期 / 连接 / 有效配置 md5 / 心跳周期 / endpoint）、`reload`（`forcePollNow` 以 md5=null 强制重拉并经幂等守卫 apply，不等长轮询超时）、`reconnect`（`reconnectNow` 重置退避重连、不清空 store/快照保 fail-static）、`resync`（依赖文件树托管 FR-14 未启用，占位提示）；命令经 `adapter.runAsync` 不上 MC 主线程、core 控制方法不碰平台库（守 ADR-0005）。并修复注册多触发点（心跳 404 / 长轮询 404 / 退避重试 / reconnect）无单飞保护导致的瞬时双注册——用 `AtomicBoolean` 单飞门 + 注册代标识收口，保证任意时刻只有一条 register→loops 在飞（并发单测覆盖连续 reconnect、reconnect 与 poll 并发、register 单飞不变量）。远程下发依赖鉴权（FR-11），本期不做。
- 文件树托管（通道B，控制面侧）：新增 `file_object` / `file_revision` 两表（整文件 blob + scope 维度 + append-only 版本，唯一键含 `path`、`content` 落 TEXT 经 GORM size 抽象不绑方言、软删哨兵同 config_item）；`internal/filetree` 纯函数包做 scope **整文件覆盖**（取覆盖链最高层那份、不深合并、不碰 merge 包）+ manifest（path→md5）+ 独立 `fileTreeMd5`（path 名纳入哈希防碰撞）；FileService（建/发布/回滚/软删，事务内 object+revision+audit 原子）+ FileEffectiveService（解析 + 文件长轮询，持独立 Hub）；admin 文件 CRUD/发布/历史/回滚端点与 agent `files/manifest`（长轮询）/`files/content` 端点；长轮询配置与文件**唤醒集合独立**（zone 改派同时唤醒两通道）；path 校验拒绝绝对路径/`..` 穿越/反斜杠防落盘逃逸。filetree 穷举单测（scope 整文件覆盖 + fileTreeMd5 幂等/防碰撞/内容敏感）全绿。agent 侧文件同步器与原子落盘（含父目录 fsync）尚未实现。
- 文件树托管（通道B，agent 侧）：`BeaconApiClient` 增 `pollFileManifest`（带本地 `fileTreeMd5` 长轮询 manifest）/`fetchFileContent`（按 path 取整文件），HTTP/JSON 仍只经 `HttpTransport`/`JsonCodec` 接口；新增 `agent-core` 的 `filetree` 包——`FileSyncer`（manifest 增/改/删纯差分）、`FileMirrorWriter`（原子写：临时文件→`FileChannel.force` 含父目录 fsync→`ATOMIC_MOVE`，`RelativePathGuard` 拒绝绝对/`..`/反斜杠逃逸目标根）、`AppliedFileManifestStore`（本地已落盘清单原子读写）、`FileTreeApplier`（先文件后清单的增量同步编排）；`AgentLifecycle` 注册成功后并行启文件树长轮询循环（独立 gen/退避、复用单飞、`adapter.runAsync` 不上 MC 主线程），**fail-static 比配置更保守**：控制面不可用/取内容失败/首启无目标态一律不动既有镜像、不写清单、绝不臆测删文件；目标根来自 config.yml `file-tree` 段（镜像落插件 plugins 基目录）。新增单测覆盖差分增改删、原子写+无 tmp 残留+路径拒绝、fail-static 不删既有/不写清单、manifest/content 解析。`resync` 命令的实际接线本期仍占位未做。
- 三方插件文件覆盖兼容（FR-15，见 [ADR-0011](docs/adr/0011-third-party-file-override-and-restricted-reload-command.md)）：在通道B 之上为无法改源码的三方插件提供 override-set（目标插件目录 + 成员文件 + 一条重载命令）。控制面新增 `file_override_set` / `file_override_set_revision` 两表（VARCHAR 枚举 + 软删哨兵 + 事务内 object+revision+audit 原子）与 admin CRUD/发布/历史/回滚 + **dry-run 只读预览**端点（全挂鉴权中间件后，FR-11）；目标根 / 成员路径 / 重载命令早校验。agent 侧新增 `override` 包：覆盖前备份（**回滚只还原不重放命令**）、`CommandWhitelist`（agent 本地白名单、默认空、单条、注入字符拒绝）、`OverridePathSecurity`（Path 级 `plugins/<plugin>/` 限定 + 拒 `..`/绝对/盘符/UNC/`.jar`/server 文件 + Windows 保留名与**段尾点/空格**专项）、`ReloadCommandExecutor`（经 `PlatformAdapter.dispatchConsoleCommand` 派发、不上 MC 主线程同步等结果、**禁 shell**——core/适配器不引入任何进程执行 API）、`ManagedFileTracker`（受管文件标记 + 外部改动告警防震荡环）。安全单测全绿，经一轮对抗式安全审查（未发现可达 RCE/穿越/鉴权绕过）。**override 应用链已接入、命令执行随白名单生效**（接线见下条）。
- 三方覆盖集命令执行接线（FR-15 接线，见 [ADR-0011](docs/adr/0011-third-party-file-override-and-restricted-reload-command.md)）：把"已建未接"的 override 应用链接入运行期——命令执行（RCE 面）正式上线，受 agent 本地白名单（默认空 = 不派发）兜底。控制面新增 agent-facing 投递端点 `GET /beacon/v1/agent/override-sets`（长轮询，带 `overrideMd5`，返回适用覆盖集的目标根 + 受限重载命令 + 成员 path，不含内容）与 `GET /beacon/v1/agent/override-sets/content`（按 `set`+`path` 取成员整文件内容），均挂 agent token 防误连；`OverrideEffectiveService` 按 scope 覆盖链解析适用集（同名取最高层、复用 fileHub 唤醒集合），覆盖集成员（`override_set_id>0`）从通用文件树清单排除以免双写错误根；覆盖集建/发布/回滚/软删事务提交后按 scope 唤醒受影响 serverId。agent 侧 `BeaconApiClient` 增 `pollOverrideSets`/`fetchOverrideMember`，新增 `OverrideSyncApplier` 编排：逐集取齐成员 → 经 `OverrideApplier`（备份 → `TargetRootSecurity` 目标根 agent 侧最终校验 + `OverridePathSecurity` 成员路径校验 → 原子覆盖 → 受管标记）→ 全量落盘成功且命中本地白名单才经 `ReloadCommandExecutor` 派发命令；`AgentLifecycle` 注册成功后并行启 override 长轮询循环（独立 gen/退避、复用单飞、`adapter.runAsync` 不上 MC 主线程）；**fail-static**：控制面不可用 / 取成员失败 / 恶意目标根一律不动既有、不派发命令、不更新基准，下轮向目标态收敛重做；**回滚只还原不重放命令**。新增 `TargetRootSecurity`（防控制面被攻破下发逃逸 plugins 的恶意目标根，agent 为最终权威，决策 4）与 `OverrideSyncApplier` 单测（落盘 + 白名单派发 / 白名单空不派发 / 取内容失败 fail-static / 恶意目标根整集拒绝 / 同 md5 幂等 / 失败后恢复）、`BeaconApiClient` 投递端点契约单测、控制面 override 投递解析 + 成员排除 + 发布唤醒集成测试，全绿。
- 下游 SDK 接入包（FR-16，见 [ADR-0007](docs/adr/0007-versioning-and-release-channels.md)）：新增纯 Java8 零三方依赖的 `agent-kit` 便捷接入层（只依赖 `agent-api`）——`BeaconAccess` 封装下游样板：回退判据只看 `BeaconAgentProvider.isAvailable()`（绝不看 connected 防 split-brain）、读已合并配置 / 查发现便捷方法、配置订阅桥（注册即重放 + 由不可用转可用补注册重放）；非有状态静态单例、不碰线程调度，身份 / zone / ORM 仍归 CoreLib 不重复。`agent-kit` 随 `agent-api` 经 shadowed 打进 BeaconAgent / BeaconAgentProxy jar（下游 compileOnly、运行期由壳提供，免 NoClassDefFound）。`agent-api` 与 `agent-kit` 配 maven-publish（group `top.wcpe.beacon`、artifact `beacon-agent-api` / `beacon-agent-kit`、version 跟随根 `VERSION`，默认 mavenLocal、远程经 `beaconPublishUrl` property/env 可选注入，不硬编码 TabooLib 仓库）。首版随 0.y.z 不承诺向后兼容（ADR-0007）。
- 管理台前端增强（FR-18）：新增登录页 + `RequireAuth` 守卫 + 401 全局拦截，`operator` 改取登录态、`client` 单点注入 `Authorization` 头。新增文件树托管页（过滤 / 新建 / 列表 + 文件详情：编辑发布 / 历史 / 并排 diff / 回滚 / 软删）与三方覆盖集页（列表 + 详情：元数据 + **发布前 dry-run 只读预览**「将覆盖哪些文件 / 执行什么命令」+ 勾选确认门控发布 + 历史 / 回滚）。文本编辑用 `CodeEditor`（textarea + 行号，不引 CodeMirror / Monaco）；不引任何新运行时依赖；不做灰度向导 / 覆盖来源徽标等（轻档）。
- SDK 与 agent-api 接入文档（FR-19）：新增 [docs/SDK.md](docs/SDK.md)——SDK 组成（agent-api + agent-kit）、发布坐标与版本对齐矩阵、`compileOnly` 接入、最小接入示例、`BeaconAccess` / `BeaconAgent` API 参考、回退判据「只看 isAvailable 不看 connected」等接入纪律。

### 变更
- 文件树托管端点写操作 `operator` 改取认证身份（`auth.Operator(ctx)`）而非请求体，与 override-set 写操作一致——`POST /admin/v1/files`、`PUT /admin/v1/files/{id}` 请求体移除 `operator` 字段，后端以登录令牌身份写审计。
- 管理台前端各写操作（配置/文件/zone/实例下线 的新建/发布/回滚/软删/指派/取消指派）不再向请求体/查询发送 `operator`——后端已统一取认证身份并忽略手填值，前端发送即冗余；同时移除随之失效的 `useOperator` hook 与 `requireOperator` 空值校验（登录后身份恒在、未登录由 `RequireAuth` 守卫拦截），登录身份仅保留侧栏「当前操作人」展示用途，行为不变。

### 修复
- `file_object` 唯一键改用定长 `path_hash`：避免 `path varchar(512)` 入 utf8mb4 复合唯一键超 MySQL 3072 字节键长致 AutoMigrate 建表失败、拖垮整库迁移。
- agent 从仓库根 `VERSION` 注入 `project.version`（兑现 ADR-0007；此前为 `unspecified`，e2e jar 名解析成空版本）。
- bungee 壳显式 `compileOnly` 引入 `net.md-5:bungeecord-api`，修复 TabooLib `install(BungeeCord)` 未把 bungee API 上编译类路径致编译失败。
- override 反馈环读盘异常（目标为目录占位 / 不可读）改为跳过该文件 + 告警，避免单文件令 override 同步异步循环静默停摆。

### 安全
- 管理面鉴权（FR-11）：admin API 由开放访问改为需登录令牌（见上方破坏性变更）；写操作 `operator` 取认证身份、不可手填伪造。
- 三方覆盖命令执行（RCE 面）随 FR-15 接线正式上线，过专门对抗式安全审查（9 条 ADR-0011 约束接线后全成立、未发现可达 RCE / 路径穿越 / 鉴权绕过 / 主线程阻塞）；受 agent 本地白名单（默认空 = 不派发任何命令）兜底、控制面不下发白名单。**已知项**：命令执行的真机端到端（配置覆盖集 + 白名单 + 对真实插件派发重载命令）尚未单列 E2E（默认 inert），生产启用白名单前建议补跑。

## 0.1.0（2026-06-17）

### 新增
- 项目立项与第一期（MVP）设计定稿：确立"控制面（Go + 内嵌 React）/ 数据面（Bukkit/Bungee agent）"架构。
- 架构文档 [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)：控制面/数据面切分、MySQL+GORM 六表数据模型、scope 覆盖链合并、REST 长轮询热更机制、docker-compose 单节点部署。
- REST 契约 [docs/API.md](docs/API.md)：agent 侧与 admin 侧端点。
- 架构决策记录 [docs/adr/](docs/adr/)：自研而非用 Nacos、Go+内嵌 React 栈、MVP 去 Redis、zone 由控制面权威指派、agent 传输/序列化抽象层、REST 长轮询推送。
- 文档治理：PRD 入库为活文档（[docs/PRD.md](docs/PRD.md)）、新增演进与维护指南（[docs/CONTRIBUTING.md](docs/CONTRIBUTING.md)）与文档同步规则（`.claude/rules/doc-sync.md`），确立"文档即代码、ADR 不可变只取代"的防漂移流程。
- 工程化补齐：版本来源与发布渠道（[ADR-0007](docs/adr/0007-versioning-and-release-channels.md) + 根 `VERSION`）、GitHub Flow 分支模型与 PR/Issue 模板、运维手册（[docs/OPERATIONS.md](docs/OPERATIONS.md)）与安全说明（[SECURITY.md](SECURITY.md)）、迭代技能补充（`publish-snapshot` / `hotfix` / `bump-dependencies`）。
- 可演进与静态检查：PRD 功能需求加"状态"列（计划 / 开发中 / 已交付@版本）作活路线图；CONTRIBUTING 增"文档如何长期演进"章节；新增代码静态检查规范（`.claude/rules/static-analysis.md` + 根 `.golangci.yml`）。
- 维护期操作手册：CONTRIBUTING 增"维护迭代周期"（工作项→技能路由 + 端到端循环）、ADR 实操指引（编号 / 何时写 / 取代示例）、文档冷热分层（高频 / 中频 / 低频 / 近乎不变）。
- 变更速查：CONTRIBUTING §10.1 加"一次变更各动哪些"表（feat/fix/重构/回滚/依赖/发版/快照/架构决策/开新阶段 → 动哪些文档与产物，含版本号 / ADR / 期数的变动频率），讲清"100 feat + 100 fix 也几乎不动期数"。
- 提交规范强化：git-commit 增两条强制约束——禁止阶段性词语（Phase/P0/MVP/Sprint/迭代，按功能点而非开发阶段描述）+ 最小提交粒度（独立可编译 / 只做一件事 / 不混 feat·fix·refactor），各附合格与不合格示例。
- 文档维护技能：新增 `update-docs`（纯文档工作：写 / 取代 ADR、原地更新架构 / API、修文档漂移、整理文档），并接入维护迭代周期路由表。
- 审计闭环修正：补 `.env.example` / `web/dist/.gitkeep`；新增 [ADR-0008](docs/adr/0008-config-soft-delete-and-effective-md5.md)（软删唯一键哨兵 + 有效配置 md5 取舍）；验证门权威判据改挂入库真源（PRD 验收 + 高风险区测试 + 组件测试绿），不再依赖不入库的实施计划；统一 agent 模块↔jar 命名；补测试分层、备份常态化与恢复演练、`govulncheck` 漏洞入口。
- 收尾：采用 [MIT 许可](LICENSE)；SECURITY 明确为内部项目、不对外接收漏洞报告；从 `.claude/rules` / `.claude/skills` 清除"M0 未落地"等过渡性措辞（稳态规则只陈述既定事实，过渡状态归 README 当前状态与 `.tmp` 计划）。
- ADR 导航：CONTRIBUTING §3.1 与 adr/README 补"ADR 保持稀少、现状看 ARCHITECTURE、取代修剪活跃集、不必通读、增长过快是滥写信号"说明。
- 功能规格：新增 `docs/specs/`（右尺寸 per-feature spec，单文件一功能 + `_template.md`）——非平凡功能开发前先写需求/设计/任务/验收；接入 `develop-feature`、CONTRIBUTING 文档地图与冷热分层（小改动免，PRD 与 spec 分工见 `docs/specs/README.md`）。
- PRD 分期去硬编码：§7 改为按主题描述各期 + 指向 §4「期」列为唯一来源，加 FR 不再需手改区间（与"状态列""ADR 编号"同理，消除双源）；并注明分期是少数粗粒度阶段、产品成熟后改按版本 + 功能组织，不会堆到上百期。
- 仓库骨架与最小可运行控制面落地：Go 控制面（chi + GORM）可起服连 MySQL、AutoMigrate、预置 prod/test 两环境并经 `GET /admin/v1/namespaces` 返回；分层 router→handler→service→repository，新增 render（统一响应/错误 + traceId）与 apperr（领域错误）两个叶子包；内嵌前端经根包 `//go:embed all:web/dist` + SPA 回退处理器提供。
- 前端骨架：Vite + React + TypeScript 空壳管理台 + apiClient（react-router + @tanstack/react-query）。
- 双端 agent 骨架：Kotlin/TabooLib 三模块 Gradle 工程（agent-core 纯 Kotlin、agent-bukkit 打包 BeaconAgent、agent-bungee 打包 BeaconAgentProxy），`gradlew build` 通过。
- 部署：多阶段 Dockerfile（node 构建前端 → go 内嵌编译 → 极小非 root 镜像）+ docker-compose（beacon + mysql，含健康检查与命名卷）+ `config.example.yaml`，`docker compose up` 可起全栈。
- 配置中心（无推送）：scope 覆盖链键级深合并内核（yaml/json/properties codec 键序稳定、标量覆盖/map 深合并/list 整替/null 删键、整体 md5 纳入 dataId 名）；config_item/config_revision/zone_assignment/audit_log 四表（软删唯一键用固定哨兵、content 经 GORM size 抽象不绑方言）；配置 CRUD/发布/历史/回滚/diff（事务内 item+revision+audit 原子，发布做格式/大小/解析与跨层 format 一致性校验），有效配置按覆盖链解析（收敛只看 md5）；`/admin/v1/configs` 全套端点；merge 穷举单测 + 真实 MySQL 全流程与四层合并集成测试。
- 注册/发现/健康 + zone 分配：`runtime.Registry`（内存 map+RWMutex、读返深拷贝、重复 serverId 按 lastHeartbeat 新鲜度守卫——故障换机不误杀）+ 单 goroutine 健康扫描（online→lost→offline，offline 保留）；agent 侧 register（解析回填 group/zone）/heartbeat/report/discovery（挂 X-Beacon-Token 中间件）；admin 侧实例列表/详情/下线、zone 指派 CRUD 与汇总（改派即时重算有效配置并刷新内存归属，长轮询唤醒留待 M3）；capacity/weight 顶层、metadata 自定义、无 canary；runtime 并发单测（-race）+ REST 与改派重算集成测试。
- 长轮询热更：`longpoll.Hub`（缓冲为 1 的 notify channel、按 serverId/namespace 唤醒）+ `config/effective` 长轮询入口"先注册 waiter 再算 md5、唤醒即重算比对"（变了 200、未变挂起、超时 304）；发布/回滚/软删/改派事务提交后按 scope 算最小受影响集合再唤醒（global→全 ns、group→查内存、zone→反查 DB、server/改派→单 serverId）；有效配置未分配回退 groupHint；Hub 并发单测 + 真实 MySQL 长轮询时序集成（立即返回/超时/唤醒/只唤醒受影响/改派热推）。
- 审计查询端点：`GET /admin/v1/audits`（按 namespace/action/targetType/targetRef/时间过滤 + 分页，时间倒序，返回 total+items），补齐管理台审计页所需后端；集成测试覆盖过滤/分页/排序。
- React 管理台：configs（过滤/新建/详情发布/历史/diff/回滚/软删）、instances（过滤 + 5 秒健康轮询 + 未分配高亮 + 下线）、zones（指派 CRUD + 汇总）、audits（过滤分页）、namespaces（列表/新建）；react-router 可深链 + @tanstack/react-query；纯 CSS 无新增依赖；经根包 `go:embed all:web/dist` + SPA 回退由单二进制同端口供 UI+API。深链刷新不 404、无头浏览器渲染确认 React 正常挂载。
- 双端 agent（Kotlin/TabooLib，五模块实现 ADR-0005）：agent-api（纯 Java8 只读 API：读有效配置 + 查发现）/ agent-core（零具体库依赖：HttpTransport·JsonCodec 接口 + BeaconApiClient 五调用 + 生命周期状态机 + 快照 + applier + 退避）/ agent-adapters（OkHttp + kotlinx，唯一碰具体库）/ bukkit·bungee 壳。生命周期 BOOTSTRAP→REGISTERING→RUNNING→DEGRADED 全异步不阻塞主线程；先点亮本地快照 fail-static、控制面挂不回退配置不阻断玩家；长轮询续杯（200 apply+report 续新 md5、304 续旧、连接失败退避重连）；插件主类 object+@Awake 不继承 JavaPlugin、身份缺失 fail-fast；okhttp/kotlinx 经 @RuntimeDependencies 运行期下载不打包、打包期 relocate 对齐其命名空间（产出 BeaconAgent / BeaconAgentProxy）。无 canary、有效配置无 version 代际号、register 的 capacity/weight 顶层、metadata 仅 map<string,string>。29 个单测（core 纯逻辑 + adapters 真实 codec 对假 transport）。真机 MC 端到端属 M6。
- 打包与端到端验收：多阶段 Dockerfile 全量构建 + `docker compose up` 起 beacon+mysql（M0/M4 已验）；新增 agent-e2e / agent-e2e-bungee 验收插件（以业务插件身份经 agent 纯 Java 只读 API `config().raw()/md5()/onChange()` 读配置并监听变更），用 TabooLib runServer/runBungee 自动下载并运行 Paper 1.20.4 + Waterfall 1.20（自动 EULA），双端跑通两条端到端时序——①首次接入（双端 instances online）②发布热更（业务插件经 API 读到 mode A→B→C，PUT 后约 3s 不重启 apply）——且审计可查（config.create/publish）。docs/OPERATIONS 增端到端验收跑法。

### 变更
- agent 包路径从 `com.beacon.agent` 迁移到 `top.wcpe.beacon.agent`（e2e 模块 `com.beacon.e2e` → `top.wcpe.beacon.e2e`）；Gradle group 同步为 `top.wcpe.beacon.agent`，第三方库经 `@RuntimeDependencies` 运行期下载不打包、其打包期/运行期 relocate 命名空间随之为 `top.wcpe.beacon.agent.lib.*`。**对 agent-api 消费方为破坏性变更**：业务插件需同步更新 `compileOnly` 坐标 group 与 `import`。并对齐 CoreLib 的 TabooLib 用法——bungee 改枚举式 `install`、双端 TabooLib 版本统一为 `6.3.0-afd75a7`、移除冗余的 `relocate("taboolib", …)`（jar 内 taboolib 命名空间不变）。
- 控制面默认 HTTP 端口由 `8080` 改为 `8848`（管理台与 agent 同端口）；web 开发代理、agent 默认 endpoint、`.env.example` / `docker-compose.yml` / `Dockerfile` 及运维文档同步收敛到 `8848`。
- 明确 P1 范围：心跳响应 `configDirty` 优化提示位有意不在 P1 实现、恒返 `false`（变更感知由长轮询负责，agent 不依赖），归档 P2；代码注释与 `docs/API.md` / `docs/PRD.md` 同步标注。
- 集成测试加 `//go:build integration` 标记与单测隔离：`go test ./...` 只跑单测、`go test -tags=integration ./...`（+ `BEACON_TEST_DSN`）才跑集成，杜绝集成被静默 `t.Skip` 误判"全绿"；运行方式见 `docs/OPERATIONS.md` §8，`testing-and-quality` 规则同步。

### 修复
- 审计来源 IP 恒空：config / zone / instance 三处审计此前未写 `client_ip`，现由 handler 提取请求来源 IP（X-Forwarded-For 首跳 → X-Real-IP → RemoteAddr）按既有显式传参约定透传至 service 审计写入，管理台"来源 IP"列可用（FR-7 / PRD §6⑧）。

> 第一期（MVP）首个正式版本：配置中心、服务注册/发现/健康/zone 分配、长轮询热更、React 管理台、双端 agent 全部落地并验收——集成测试真跑全 PASS（`-tags=integration` + MySQL），PRD §6 八条真机真集群逐条通过（秒级 apply / 回滚 / 三层合并 / zone 热推 / 健康 TTL / 杀控制面 fail-static + 重连 / 同 serverId 守卫 + 故障换机不误杀 / 审计可查），FR-1~8 已交付@v0.1.0。
